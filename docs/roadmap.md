# Roadmap

FIAP X evolves from the current synchronous monolith (`main.go`) into a fully structured DDD system across exactly **8 phases**. Each phase decomposes into one or more focused OpenSpec changes — see [Change Backlog](#change-backlog) below for the concrete, right-sized units and their order. A phase is a grouping for human understanding, not a promise that it ships as a single change.

> **Canonical source:** The authoritative roadmap — including full scope descriptions, ADRs, dependency rules, and the complete domain model — lives in [`openspec/specs/ddd-architecture/spec.md`](../openspec/specs/ddd-architecture/spec.md). Phase 2's own scope is likewise authoritative in [`openspec/specs/identity-authentication/spec.md`](../openspec/specs/identity-authentication/spec.md).
>
> This file is a summary for human readers. If the Phase Summary or any architecture/target-state description below conflicts with the canonical source, the canonical source takes precedence.
>
> The **Change Backlog** section below is different: it's a planning/sequencing artifact with no counterpart in `openspec/specs/` — canonical specs describe target system behavior, not the order in which OpenSpec changes get proposed. It can't "conflict" with the canonical source in the same sense; it's authoritative for change sequencing in its own right. See [`openspec/specs/development-workflow/spec.md`](../openspec/specs/development-workflow/spec.md) for how it's maintained.

## Phase Summary

| Phase | Scope | Status |
|---|---|---|
| **1** | OpenSpec artifacts only: bounded contexts, `VideoJob` aggregate, package topology, ADRs, dependency rules, frontend/presentation layer contract, permanent project documentation. No application code changes. Shipped as a single change, `establish-ddd-architecture-foundation`. | Done |
| **2** | `internal/identity/domain` and `application`; JWT bearer middleware; `RegisterUser` and `AuthenticateUser` use cases; PostgreSQL adapter for Identity; explicit per-artifact ownership enforcement. Wired into the existing `main.go` composition root rather than a new `cmd/api` — that migration stays in Phase 3. Shipped as a single change, `implement-identity-authentication-from-scratch`; a design correction is now tracked separately (see Change Backlog). | Done, correction pending |
| **3** | `internal/video/domain` and `application`; PostgreSQL schema for `video_jobs` and `outbox`; `CreateVideoJob` and `GetJobStatus` use cases; synchronous `ffmpeg` call migrated from `main.go` into application layer; frontend extracted from `getHTMLForm()` into `web/index.html`, `web/styles.css`, `web/app.js`. Decomposed into multiple changes — see Change Backlog. | Done |
| **4** | Redis adapter (`internal/platform/redis` — shipped, see Change Backlog); idempotency keys on `POST /upload` (shipped, see Change Backlog); rate-limiting middleware (shipped, see Change Backlog); status cache for `GetJobStatus` (shipped, see Change Backlog). PostgreSQL remains source of truth. A design correction to the idempotency feature's Redis-error handling has also shipped (`fail-open-upload-idempotency`, see Change Backlog). | Done |
| **5** | MinIO storage adapter behind the `StoragePort` interface; migrate upload and result storage from local filesystem to MinIO; presigned download URLs. Decomposed into four changes — see Change Backlog. | Done |
| **6** | RabbitMQ infrastructure; `EnqueueVideoJob` publishes to queue; transactional outbox relay; `cmd/worker` picks up messages and runs `ffmpeg`; distributed lock (Redis-backed) preventing concurrent worker pickup of the same job; `POST /upload` becomes non-blocking (returns job ID immediately); `web/app.js` updated for async polling flow. Decomposed into four changes — see Change Backlog. | Planned |
| **7** | `internal/notification/domain` and `application`; RabbitMQ event subscription; email and webhook delivery; `NotificationPreference` per user; retry logic and HMAC webhook signatures. Not yet decomposed. | Planned |
| **8** | Structured logging; Prometheus metrics; `/health` and `/ready` endpoints; `docker-compose.yml` for full local dev stack (PostgreSQL, Redis, RabbitMQ, MinIO). Not yet decomposed. Dockerfile hardening was originally planned here but has been pulled forward — see `harden-dockerfile` in the Change Backlog. | Planned |

## Change Backlog

This is the single source of truth for the product/architecture-scope OpenSpec work ahead — the 8-phase DDD roadmap above, decomposed to the finer-grained `openspec/changes/<name>/` level. For which changes get a row here, when this document is touched, and the rest of the OpenSpec/PR process, see [`openspec/specs/development-workflow/spec.md`](../openspec/specs/development-workflow/spec.md).

### Phase 2 corrections (already shipped — fixing a design mistake)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `enforce-mandatory-identity-config` | Remove the "runs unauthenticated when entirely unconfigured" path (`setupIdentity`'s current behavior). Startup must fail without both `IDENTITY_POSTGRES_DSN` and `IDENTITY_JWT_SIGNING_KEY`. Every protected route always requires a valid bearer token — no bypass. Corrects the "entirely unconfigured runs video-processing only" scenario in `openspec/specs/identity-authentication/spec.md`. | none | [archived](../openspec/changes/archive/2026-08-10-enforce-mandatory-identity-config/), promoted into [`openspec/specs/identity-authentication/spec.md`](../openspec/specs/identity-authentication/spec.md) |

### Frontend (orthogonal to everything else, low risk)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `extract-frontend-to-static-files` | Move `getHTMLForm()`'s HTML/CSS/JS out of `main.go` into `web/index.html`, `web/app.js`, `web/styles.css`, served via `go:embed`. No behavior change. | none | [archived](../openspec/changes/archive/2026-08-11-extract-frontend-to-static-files/), promoted into [`openspec/specs/ddd-architecture/spec.md`](../openspec/specs/ddd-architecture/spec.md) |

### Local development tooling (orthogonal to everything else, low risk)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `add-docker-compose-app-service` | Add an `app` service to `docker-compose.yml` alongside the existing `postgres` service: builds the existing Dockerfile, wires `IDENTITY_POSTGRES_DSN`/`IDENTITY_JWT_SIGNING_KEY` over the compose network, gated on Postgres's healthcheck so it doesn't hit the fail-fast unreachable-DB error before Postgres is ready, bind-mounts `uploads/`/`outputs/` to the host, publishes the port loopback-only. `docker-compose.yml` becomes the **sole** documented Docker workflow — the plain `docker build`/`docker run` path and the separate Docker-based test fallback are retired from documentation, not kept alongside it (`docs/operations.md`'s deployment-focused Docker section is the one exception, explicitly out of scope). A separate `identity_test` database (via a postgres init script) keeps the test command from truncating the running app's data. Config/docs only, no application code. | none | [archived](../openspec/changes/archive/2026-08-08-add-docker-compose-app-service/), promoted into [`openspec/specs/development-workflow/spec.md`](../openspec/specs/development-workflow/spec.md) |

### Dockerfile hardening (pulled forward from Phase 8, orthogonal, low risk)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `harden-dockerfile` | Replaced the single-stage, root-user `Dockerfile` (documented anti-pattern; ran `go mod tidy` at build time against whatever the module proxy currently serves, rather than resolving deterministically from the committed `go.sum`, and shipped the full Go toolchain in the final image since there was no separate runtime stage) with a three-stage build: `builder` (read-only `go.sum`-verified deps, static binary), `test` (`builder` + `ffmpeg`, backs the new `docker-compose.yml` `app-test` service), and `runtime` (pinned `alpine:3.24`, `ffmpeg`, non-root UID 1000, no Go toolchain). Pulled forward from Phase 8 at the user's explicit request — no longer bundled with that phase's observability work. | `add-docker-compose-app-service` | [archived](../openspec/changes/archive/2026-08-09-harden-dockerfile/), promoted into [`openspec/specs/container-image/spec.md`](../openspec/specs/container-image/spec.md) and [`openspec/specs/development-workflow/spec.md`](../openspec/specs/development-workflow/spec.md) |

### Phase 3 — Video Processing persistence & `cmd/api` extraction

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `add-videojob-domain-and-application` | `VideoJob` aggregate, state machine, value objects, repository/port interfaces, and exactly three use cases: `CreateVideoJob`, `GetJobStatus`, `ListUserJobs`. No infra, no HTTP. None of the three use cases in scope trigger a state transition (`CreateVideoJob` only creates in `pending`), so `EnqueueVideoJob`, `StartProcessing`, `CompleteJob`, and `FailJob` are all out of scope here. Per the canonical state machine (`openspec/specs/ddd-architecture/spec.md`), `CompleteJob`/`FailJob` are only valid from `processing`, which is reachable solely via `EnqueueVideoJob` then `StartProcessing` — so all four transition use cases are added together by `migrate-ffmpeg-execution-to-videojob-application` below, calling all four in sequence synchronously in-process (no real queue/worker yet). Phase 6 later replaces that in-process sequencing with an actual RabbitMQ-backed queue and a separate `cmd/worker`, not with new transition logic. | none | [archived](../openspec/changes/archive/2026-08-16-add-videojob-domain-and-application/), promoted into [`openspec/specs/ddd-architecture/spec.md`](../openspec/specs/ddd-architecture/spec.md) and [`openspec/specs/videojob-lifecycle/spec.md`](../openspec/specs/videojob-lifecycle/spec.md) |
| `add-videojob-infrastructure` | PostgreSQL adapter for `VideoJob`, jobs + outbox schema/migration. | `add-videojob-domain-and-application` | [archived](../openspec/changes/archive/2026-08-16-add-videojob-infrastructure/), promoted into [videojob-persistence spec](../openspec/specs/videojob-persistence/spec.md) |
| `extract-cmd-api-entrypoint` | Move the HTTP composition root from `main.go` into `cmd/api`, preserving current routes/behavior 1:1. | `extract-frontend-to-static-files` (no Go string literal to drag along) | [archived](../openspec/changes/archive/2026-08-16-extract-cmd-api-entrypoint/), promoted into [`openspec/specs/ddd-architecture/spec.md`](../openspec/specs/ddd-architecture/spec.md) |
| `wire-videojob-http-endpoints` | New job-oriented endpoints backed by `VideoJob`, added alongside the legacy synchronous flow (not yet replacing it — this row only wires the new read/create paths). Note: jobs created through these new endpoints have no processing trigger through the rest of Phase 3 either — `migrate-ffmpeg-execution-to-videojob-application` below only migrates the legacy `POST /upload` handler's `ffmpeg` call, not this row's new create endpoint, so a job created here stays stuck in `pending` even after that row ships. Acceptable only because the frontend keeps using the legacy `/upload` flow throughout Phase 3, so it doesn't affect real usage — but the new endpoints must not be presented or documented as a working end-to-end path until a later change explicitly wires their processing trigger too. | `add-videojob-infrastructure`, `extract-cmd-api-entrypoint` | [archived](../openspec/changes/archive/2026-08-17-wire-videojob-http-endpoints/), promoted into [videojob-http-api spec](../openspec/specs/videojob-http-api/spec.md) |
| `migrate-ffmpeg-execution-to-videojob-application` | Cut `POST /upload`'s `ffmpeg` invocation over to run through the `VideoJob` application layer (still synchronous — no queue/worker until Phase 6) and retire the legacy in-`main.go`/`cmd/api` exec path. Adds `EnqueueVideoJob`, `StartProcessing`, `CompleteJob`, and `FailJob` to `internal/video/application` (all deferred by `add-videojob-domain-and-application` above since nothing called them yet), calling all four synchronously in-process — `pending → queued → processing → completed`/`failed` — around the `ffmpeg` invocation, satisfying the canonical state machine's requirement that completion/failure only follow `processing`. This is what actually fulfills Phase 3's "synchronous `ffmpeg` call migrated from `main.go` into application layer" promise; without this row the prior four only add a parallel path and never complete Phase 3. Known consequence, not a defect: `/upload` now creates real `VideoJob` rows, so `GET /api/video-jobs`/`GET /api/video-jobs/:id` start showing `completed`/`failed` jobs for a user who has used `/upload`, alongside any still-`pending` jobs created directly via `POST /api/video-jobs` (whose own "no processing trigger" guarantee is unaffected — this row does not touch that endpoint). | `wire-videojob-http-endpoints` | [archived](../openspec/changes/archive/2026-08-17-migrate-ffmpeg-execution-to-videojob-application/), promoted into [videojob-lifecycle](../openspec/specs/videojob-lifecycle/spec.md), [videojob-execution](../openspec/specs/videojob-execution/spec.md), [videojob-persistence](../openspec/specs/videojob-persistence/spec.md), and [videojob-http-api](../openspec/specs/videojob-http-api/spec.md) specs |

### Phase 4 — Redis infrastructure & performance/reliability features

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `add-redis-infrastructure` | `internal/platform/redis`: `Config`/`LoadConfigFromEnv` (reads `REDIS_ADDR`), `Open` (bare client construction, no connectivity check), `Ping` (health check), `Close`. New `internal/platform/` namespace for cross-cutting infrastructure owned by no single bounded context. `docker-compose.yml`/CI gain a `redis` service, consumed only by this package's own tests. No `cmd/api`/`cmd/worker` wiring — the three Phase 4 features (idempotency keys, rate limiting, status cache) are separate, not-yet-proposed changes that consume this adapter. Also rescoped `ddd-architecture`'s "Redis Responsibilities" requirement to three Phase 4 responsibilities, deferring the fourth (distributed worker lock) to Phase 6. | none | [archived](../openspec/changes/archive/2026-08-18-add-redis-infrastructure/), promoted into [`openspec/specs/redis-infrastructure/spec.md`](../openspec/specs/redis-infrastructure/spec.md) and [`openspec/specs/ddd-architecture/spec.md`](../openspec/specs/ddd-architecture/spec.md) |
| `add-upload-idempotency-keys` | Content-hash idempotency for `POST /upload`: `IdempotencyKey` value object + `IdempotencyStore` port (`internal/video/domain`), `RedisStore` adapter (`internal/video/infrastructure/idempotency`) backed by `internal/platform/redis`. `handleVideoUpload` hashes the upload (SHA-256) while saving it, reserves a key scoped to `UserID` + content hash before calling `CreateVideoJob`, and a losing duplicate request polls for the winner's job status (bounded retry) instead of running `ffmpeg` again. `REDIS_ADDR` becomes required at `cmd/api` startup. First of Phase 4's three features to ship. | `add-redis-infrastructure` | [archived](../openspec/changes/archive/2026-08-21-add-upload-idempotency-keys/), promoted into [`openspec/specs/upload-idempotency/spec.md`](../openspec/specs/upload-idempotency/spec.md) |
| `add-rate-limiting-middleware` | Per-authenticated-user request rate limiting on every route gated by `identity.requireBearerAuth()`: a Redis-backed fixed-window `Limiter` (`internal/platform/ratelimit`, atomic `INCR`+conditional-`EXPIRE` via a Lua script, mirroring `idempotency.RedisStore`'s atomicity pattern) mounted as `cmd/api/ratelimit.go`'s `rateLimitMiddleware` on the `videoRoutes` group, immediately after the auth middleware. Excess requests get `429` + `Retry-After` (derived from Redis `PTTL`, rounded up to whole seconds) before any handler runs; a limiter failure — or an internal bounded per-call timeout firing — fails open rather than blocking otherwise-healthy traffic. Thresholds (`RATE_LIMIT_MAX_REQUESTS`/`RATE_LIMIT_WINDOW_SECONDS`, defaults 60/60s) are optional, unlike `REDIS_ADDR`. Second of Phase 4's three features to ship. | `add-redis-infrastructure` | [archived](../openspec/changes/archive/2026-08-21-add-rate-limiting-middleware/), promoted into [`openspec/specs/rate-limiting/spec.md`](../openspec/specs/rate-limiting/spec.md) |
| `add-videojob-status-cache` | Cache-aside/write-through Redis cache for `VideoJob` lookups: `CachedVideoJobRepository` (`internal/video/infrastructure/cache`) decorates the PostgreSQL `VideoJobRepository`, wired into `cmd/api/video.go`'s `setupVideo` ahead of every use case (`GetJobStatus`'s repeated `GET /api/video-jobs/:id` polling reads are the main beneficiary). `FindByID` reads cache-aside (a hit skips PostgreSQL; a miss/error/corrupted entry always falls back to PostgreSQL unconditionally, then attempts repopulation via `SET NX`, never a plain `SET`, so a slow reader can't clobber a fresher write-through value — a race caught in review). Repopulation is guaranteed on a true miss or a malformed-but-string entry (self-healed via a compare-and-delete Lua script first) but only best-effort on a generic Redis error (e.g. `WRONGTYPE`), since `SET NX` can't replace an existing key of the wrong type — PostgreSQL correctness is unaffected either way. `Update` writes through with an unconditional `SET` after the PostgreSQL write succeeds. Fixed 5-minute TTL, not configurable — no new environment variable. `FindByUserID`/`Create` are intentionally uncached. Third and final of Phase 4's three features to ship — Phase 4 is now complete. | `add-redis-infrastructure` | [archived](../openspec/changes/archive/2026-08-21-add-videojob-status-cache/), promoted into [`openspec/specs/videojob-status-cache/spec.md`](../openspec/specs/videojob-status-cache/spec.md) |

### Phase 4 corrections (already shipped — fixing a design mistake)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `fail-open-upload-idempotency` | `POST /upload`'s idempotency reservation (`RedisStore.Reserve`) previously failed closed on a Redis error — the request returned `500` instead of proceeding. Corrects this to fail open, matching rate limiting and the status cache: a `Reserve` error is logged and the request proceeds to `CreateVideoJob` without dedup protection, so a Redis outage degrades an optimization instead of blocking the critical path (video processing) — decisive given this system is being prepared to run multiple `cmd/api` replicas, where fail-closed would otherwise become a fleet-wide synchronized outage. Modified the existing "Concurrent Identical Requests Are Serialized" requirement in `openspec/specs/upload-idempotency/spec.md`. | `add-upload-idempotency-keys` | [archived](../openspec/changes/archive/2026-08-22-fail-open-upload-idempotency/), promoted into [`openspec/specs/upload-idempotency/spec.md`](../openspec/specs/upload-idempotency/spec.md) |

### Phase 5 — Object storage migration

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `add-minio-infrastructure` | `internal/video/infrastructure/storage`: `Config`/`LoadConfigFromEnv` (reads `VIDEO_MINIO_ENDPOINT`, `VIDEO_MINIO_ACCESS_KEY`, `VIDEO_MINIO_SECRET_KEY`, `VIDEO_MINIO_BUCKET`, optional `VIDEO_MINIO_USE_SSL`), `Open` (client construction, no connectivity check, but an `error` return unlike `redis.Open` — the underlying constructor parses the endpoint and builds the transport; it does **not** validate credentials), `Ping` (a real `BucketExists` round trip, not `minio-go`'s cached `IsOnline`), and `EnsureBucket` (idempotent, concurrency-safe). No `Close`: `*minio.Client` exposes no teardown and keeps its transport unexported, so a wrapper could only return `nil` while releasing nothing. The package sits in the Video Processing context rather than `internal/platform/`, following the PostgreSQL precedent — `internal/identity/infrastructure/postgres` and `internal/video/infrastructure/postgres` are separate context-owned packages against one physical database — since every MinIO consumer is Video Processing; it therefore modifies no canonical spec. `docker-compose.yml`/CI gain a `minio` service, consumed only by this package's own tests; unlike `redis`, it gets a named volume, since from the next change onward the bucket holds the only durable copy of a completed job's zip. CI starts it via `docker run` rather than a `services:` entry — verified against the pinned image, whose `CMD` lacks `server /data`, which a service container cannot supply. No `cmd/api` wiring and no new required startup configuration. | none | [archived](../openspec/changes/archive/2026-08-23-add-minio-infrastructure/), promoted into [`openspec/specs/minio-infrastructure/spec.md`](../openspec/specs/minio-infrastructure/spec.md) |
| `migrate-result-storage-to-minio` | `ResultStorage` domain port (`Put`/`Open`/`Stat`, plus `ErrResultNotFound`/`ErrResultStoreFailed`) and its MinIO adapter in `internal/video/infrastructure/storage`. `FrameExtractor` returns the local zip path instead of a `StorageKey`; `ProcessVideoJob` owns the upload, the key derivation, and the local file's lifetime, so the `ffmpeg` adapter writes only under `temp/`. `GET /download/:filename` streams from the bucket, authorized from the `VideoJob` row — the requested key must parse to a job that belongs to the caller, is `completed`, and records that exact key — with every rejection returning a byte-identical `404`. `GET /api/status` lists the caller's `completed` jobs via a new unpaginated `FindCompletedByUserID`, taking each result's `size` and `created_at` from the object itself so the field keeps meaning "when the artifact was written". Object keys stay flat (`frames_<jobID>.zip`): `app.js` uses the key verbatim as that route's single path segment, so a `/` would percent-encode and break the match — per-user prefixes need a frontend change. Retires `outputs/`, the `/outputs` static mount, and every result-side `.owner` sidecar; the sidecar helpers survive for `uploads/`. **Breaking:** the four required `VIDEO_MINIO_*` variables become a fail-closed startup precondition, and both endpoints lose their (already unreachable) unauthenticated fallbacks. | `add-minio-infrastructure` | [archived](../openspec/changes/archive/2026-08-23-migrate-result-storage-to-minio/), promoted into [`openspec/specs/videojob-result-storage/spec.md`](../openspec/specs/videojob-result-storage/spec.md) |
| `migrate-upload-storage-to-minio` | `SourceStorage` domain port (`Put`/`Get`/`Delete`, plus `ErrSourceNotFound`/`ErrSourceStoreFailed`) and its MinIO adapter, sharing one client and bucket with `ResultStorage`. `POST /upload` streams the multipart part straight into the bucket under `uploads/<uploadID>_<filename>`, hashing on the same pass — nothing touches local disk inbound — with an explicit 16MiB `PartSize`, since an unknown-size `PutObject` otherwise allocates a ~537MiB buffer per upload. `ProcessVideoJob.Execute` takes a source `StorageKey` instead of a local path (the seam Phase 6's `cmd/worker` needs, since it shares no filesystem with `cmd/api`), downloads to `temp/<jobID>_source` — no extension, so no path component derives from a user-supplied filename and `ffmpeg` probes content instead — and owns that copy's lifetime; the `ffmpeg` adapter is unchanged. Source keys carry an `uploads/` prefix precisely because no route exposes them, the complementary reason results must stay flat. Source objects are transient: one `defer` in the handler deletes each request's own on every exit path, success and failure alike, closing `video-frame-extraction`'s documented retention gap — best effort, logged with the key on failure, backstopped by an operator-side lifecycle rule. Retires `uploads/`, the `/uploads` static mount, and the entire `.owner` sidecar mechanism. **Breaking:** `/uploads` is gone, and a failed job no longer retains its uploaded video anywhere. | `migrate-result-storage-to-minio` | [archived](../openspec/changes/archive/2026-08-24-migrate-upload-storage-to-minio/), promoted into [`openspec/specs/videojob-source-storage/spec.md`](../openspec/specs/videojob-source-storage/spec.md) |
| `add-presigned-download-urls` | `GET /download/:filename` stops proxying result bytes and becomes an issuer: it keeps its route, its bearer gate, and all five entitlement conditions (evaluated against the undecorated PostgreSQL repository), `Stat`s the object, and returns `200 {"url", "expires_at"}` carrying a five-minute presigned URL the client redeems against MinIO directly. That `Stat` is load-bearing — signing is offline and succeeds for a key holding no object, so without it a missing object would surface as MinIO's own XML `404` at a different origin instead of this endpoint's byte-identical one. Because an issued URL carries no identity, issuance is the *complete* authorization decision and the grant cannot be revoked before it expires; the URL is treated as a credential (never logged, echoed, or persisted — the `StorageKey` is logged instead) and every response, success and rejection alike, carries `Cache-Control: no-store`. `expires_at` is read off the URL's own `X-Amz-Date`/`X-Amz-Expires` rather than computed as `now + TTL`, because the signing library stamps at whole-second precision and truncates the lifetime, so the naive value overstates the window MinIO admits. `ResultStorage.Open` is removed (the handler was its only caller) and replaced by a presigning operation; the adapter gains a second, presign-only client built against the new optional `VIDEO_MINIO_PUBLIC_ENDPOINT`/`VIDEO_MINIO_PUBLIC_USE_SSL` (each defaulting to its internal counterpart) plus a region discovered at startup — SigV4 signs `Host`, so the browser-facing address must be right at signing time, and without a preset region the library would try to discover one over the network against exactly that unreachable host. `app.js` navigates to the issued URL instead of building a Blob. **Breaking:** the endpoint returns JSON, not the archive. Accepted consequences: an issued URL is not revocable, and result bytes are no longer rate limited, since the transfer never reaches this API. | `migrate-result-storage-to-minio` | [archived](../openspec/changes/archive/2026-08-28-add-presigned-download-urls/), promoted into [`openspec/specs/videojob-result-storage/spec.md`](../openspec/specs/videojob-result-storage/spec.md), [`openspec/specs/video-frame-extraction/spec.md`](../openspec/specs/video-frame-extraction/spec.md), [`openspec/specs/minio-infrastructure/spec.md`](../openspec/specs/minio-infrastructure/spec.md), [`openspec/specs/video-processing-access/spec.md`](../openspec/specs/video-processing-access/spec.md), and [`openspec/specs/rate-limiting/spec.md`](../openspec/specs/rate-limiting/spec.md) |

### Phase 6 — Asynchronous processing

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `add-rabbitmq-infrastructure` | Two packages, split on the line `ddd-architecture` already draws for `internal/platform/` — connection and lifecycle plumbing, never a specific context's use case. `internal/platform/rabbitmq` holds `Config`/`LoadConfigFromEnv` (reads `RABBITMQ_URL`), `Open`, `Ping`, `Close`, a generic `Topology` descriptor with **no default value**, and `DeclareTopology(conn, topo)`; it names no exchange, queue, or routing key, and tests enforce both that it imports nothing outside `internal/platform/` and that it contains no context name. `internal/video/infrastructure/messaging` holds `JobDispatchTopology()` and the names. `Open` **connects**, unlike `redis.Open` and `storage.Open`: AMQP has no lazy client, so a deferred-connect shim would only move a diagnosable startup failure to an arbitrary later point. `Ping` opens and closes a channel rather than reading `IsClosed()`, which reports what the process has already observed — stale for exactly the unreachable-broker case a health check exists to catch. The generation suffix went on the **exchange** (`video.jobs.v1`), not the queue: a `direct` exchange delivers each publish to every queue bound with the matching routing key, so versioning only the queue would let a not-yet-redeployed replica reach a later generation's consumer — the correction that makes the cutover row's "separately named queue" workable, and which that row's own proposal has to carry. Both queues are bounded by **length**; the job queue deliberately carries **no message TTL**, because an expired message is dead-lettered without touching the `video_jobs` row and the state machine has no edge out of `queued` except to `processing`, so the job would report `queued` forever. `reject-publish` (not `-dlx`, and not `drop-head`) turns a full queue into back-pressure the publisher retries out of. `docker-compose.yml` and CI gain a `rabbitmq` service, with a named volume **and a pinned hostname** (RabbitMQ keys its Mnesia directory by hostname, so the volume does nothing without it) and a dedicated `video` account (RabbitMQ confines the built-in `guest` to loopback as the broker itself sees it). No `cmd/api`/`cmd/worker` wiring, no new required startup configuration, and no canonical spec modified — the package split is what keeps `ddd-architecture`'s `internal/platform/` scenario true rather than needing a delta against it. | none | [archived](../openspec/changes/archive/2026-08-29-add-rabbitmq-infrastructure/), promoted into [`openspec/specs/rabbitmq-infrastructure/spec.md`](../openspec/specs/rabbitmq-infrastructure/spec.md) and [`openspec/specs/videojob-messaging/spec.md`](../openspec/specs/videojob-messaging/spec.md) |
| `add-videojob-source-key-and-outbox-relay` | Three inseparable pieces. **(1)** The `VideoJob` aggregate gains a **source** `StorageKey` (new `video_jobs.source_key` column; `CreateVideoJobInput` gains the key) — a precondition, not a convenience: the worker needs `(jobID, sourceKey)`, the source key embeds a generated `uploadID` and so is not reconstructible from any existing column, and `video_jobs.storage_key` holds the **result** key set at `CompleteJob` and must not be overloaded. **(2)** `EnqueueVideoJob` moves to where `docs/domain-model.md`'s use-case table already puts it — actor “API (post-upload)”, post-condition “`queued`; message published” — so `POST /upload` performs the `pending → queued` transition itself and writes a `VideoJobQueued` outbox row in the **same transaction** — persisted `event_type` `video_job.queued`, following the `video_job.created` constant already in `internal/video/infrastructure/postgres`, and shared as one constant between the insert and the relay's filter so the two cannot drift apart silently, through a dedicated repository method rather than by making the existing `Update` outbox-aware; `Update`'s documented “writes no outbox row” contract survives untouched, which matters because `CompleteJob`/`FailJob` go through it too and Phase 7 will want their events there on its own terms. `CachedVideoJobRepository`, the decorator wrapping this repository, has to implement the new port method as well. This is also why `ProcessVideoJob` drops its own `EnqueueVideoJob` call here rather than in the cutover row — leaving it would ask the domain for a rejected `queued → queued` transition on the very next line — leaving its sequence `StartProcessing` → fetch → extract → store, and requiring a MODIFIED delta against `videojob-execution`, whose text says it “SHALL call `EnqueueVideoJob`, then `StartProcessing`”. **(3)** The relay the `video_job_outbox` table has been waiting for since Phase 3: a goroutine in `cmd/api` polling `WHERE event_type = 'video_job.queued' AND published_at IS NULL` — scoped to the event type on purpose, since the table has accumulated an unpublished `VideoJobCreated` row for every job created since Phase 3 and an unfiltered claim would re-read that backlog on every poll and starve the rows that matter (a supporting index comes with it) — publishing to RabbitMQ and stamping `published_at`, with `SELECT … FOR UPDATE SKIP LOCKED` as its concurrency guard — this system is being prepared to run multiple `cmd/api` replicas, each of which would otherwise relay the same row — deliberately a PostgreSQL-side guard, not the Redis lease, which is scoped to worker job pickup and arrives two rows below. Only `video_job.queued` rows are dispatched to the job queue; `VideoJobCreated` stays the internal event `docs/domain-model.md` already calls it, and `VideoJobCompleted`/`VideoJobFailed` remain Phase 7's, unwritten by any code. The residue this leaves between merges would be **toxic if anything ever consumed it**: after this change `/upload` marks a job `queued` and the relay publishes it while the same request still runs `ProcessVideoJob` inline, drives the job on to `completed`/`failed`, and deletes its source — so every queued message names a job already past `queued` whose source object is gone. Nothing consumes it, and that is the design rather than an accident: the cutover below declares and consumes a **separately named exchange and queue** instead of purging this one, so no residue can reach the worker at all and no purge has to be timed against a deployment in which an old replica can still publish after it runs. What the residue does **not** do is drain on its own, and the row above is why: `add-rabbitmq-infrastructure` shipped the job queue bounded by length with **no message TTL**, because an expired message would be dead-lettered without touching its `video_jobs` row and the state machine has no edge out of `queued` except to `processing`. So the residue sits in `video.jobs.queued.v1` until the cutover deletes that queue, and if it reaches the queue's maximum length first, `reject-publish` stalls this relay with its outbox rows unstamped — nothing lost, uploads unaffected because they still complete in-request, and it resumes on retirement. | `add-rabbitmq-infrastructure` | [archived](../openspec/changes/archive/2026-08-30-add-videojob-source-key-and-outbox-relay/), promoted into [`openspec/specs/videojob-outbox-relay/spec.md`](../openspec/specs/videojob-outbox-relay/spec.md) (new), [`openspec/specs/videojob-lifecycle/spec.md`](../openspec/specs/videojob-lifecycle/spec.md), [`openspec/specs/videojob-persistence/spec.md`](../openspec/specs/videojob-persistence/spec.md), [`openspec/specs/videojob-execution/spec.md`](../openspec/specs/videojob-execution/spec.md), [`openspec/specs/videojob-messaging/spec.md`](../openspec/specs/videojob-messaging/spec.md), and [`openspec/specs/rabbitmq-infrastructure/spec.md`](../openspec/specs/rabbitmq-infrastructure/spec.md) |
| `migrate-upload-to-async-processing` | The cutover, deliberately one change: a merged `cmd/worker` consuming the queue while `POST /upload` still runs `ProcessVideoJob` inline would process every upload twice. New `cmd/worker` entrypoint consumes the job queue and runs `ProcessVideoJob` → `CompleteJob` against the message's source key — the seam `migrate-upload-storage-to-minio` gave `ProcessVideoJob` its storage-key signature for, since the worker shares no filesystem with `cmd/api`. It calls `CompleteJob` on success **only**: `ProcessVideoJob` already calls `FailJob` itself for fetch, extraction, and storage failures, so a worker that also called `FailJob` would be asking the domain for a rejected `failed → failed` transition. Errors arising before processing begins — an unparseable message, an unknown job, a job it cannot claim — are rejected without requeue and so dead-lettered, never turned into a second `FailJob` and never simply acked away: an acknowledged message is gone from the broker, which would leave nothing to enumerate afterwards. **The claim must be atomic.** The outbox relay is necessarily at-least-once (a publish can succeed before `published_at` commits) and `Repository.Update` is a read-then-unconditional-write, so two deliveries would otherwise both pass `queued → processing` and both run `ffmpeg`. `StartProcessing` — `ProcessVideoJob`'s own first step now that the enqueue transition has moved to the API — therefore persists through a conditional update (`… WHERE id = $1 AND status = 'queued'`) — a new persistence path the repository port has to expose and `CachedVideoJobRepository` has to carry, the same decorator obligation the row above incurs for the outbox write; zero rows affected means another consumer won the race. `ProcessVideoJob` already returns a plain error from a failed `StartProcessing` without touching `FailJob`, so a lost claim mutates nothing and the worker rejects the message to the dead-letter queue. That is the correctness primitive: no duplicate delivery can corrupt state or run `ffmpeg` twice. It is not a *recovery* primitive, and this row should not pretend otherwise — a worker that dies mid-`ffmpeg` leaves its job in `processing`, the redelivery cannot claim a non-`queued` row, and the job stays stranded until `add-worker-job-lock` adds the fenced takeover. That residual gap is deliberate and bounded: unlike the double-processing hazard that made this row one change rather than two, it fires only on a worker crash rather than on every upload, and the next row closes it. The symptom is operator-visible (a job stuck in `processing`) and the dead-letter queue keeps the abandoned messages enumerable in the meantime. `POST /upload` stops blocking: it stores the source, creates and enqueues the job, and returns `202` with the job ID and a status URL; `cmd/api/web/app.js` gains the polling loop, whose interval must leave headroom under the per-user rate limit (default 60 requests/60s), which the upload, every poll, and the eventual download issuance all share — polling being the dominant consumer. Ownership of the source object moves from the handler's `defer` to the worker, on success and terminal failure alike, with a consequence to state plainly rather than discover: a job never picked up (relay down, worker crash before ack, message dead-lettered) now leaks its source object, so `docs/operations.md`'s `uploads/`-prefix lifecycle rule stops being a backstop and becomes the only guarantee. The idempotency reply changes meaning too — a duplicate upload now returns a `queued`/`processing` job, not a finished one — and, less obviously, the **failure-side cleanup loses its owner**: `upload-idempotency`'s “A Failed Job Clears Its Idempotency Key Immediately” requirement names `handleVideoUpload` as the actor, and that handler holds the only copy of the reservation token `Clear` needs. Once it returns `202` it never learns the outcome, so failed content would stay deduplicated for the full 24-hour window instead of being immediately retryable. Either the key and token travel to the worker (two more columns beside the `source_key` one, the cheap path) or the clear moves to the read side, where a duplicate whose referenced job is `failed` is treated as fresh; the change picks one in `design.md`, but a MODIFIED delta against that requirement is in scope either way. The cutover does **not** purge the previous row's queue — it publishes to and consumes from a new one under a different name, declared with the same bounds. A purge would have to be timed against a deployment in which an old replica can still publish toxic messages after it runs and a new one can publish legitimate jobs before it; a separate queue is meant to make that race impossible instead of documenting a sequence someone has to get right. Two corrections this row's own proposal must settle, both recorded in `add-rabbitmq-infrastructure`'s archived `design.md`: **retirement is an explicit deletion, not expiry** — the shipped job queue carries no message TTL (an expired message would be dead-lettered without touching its `video_jobs` row, against a state machine with no edge out of `queued` except to `processing`), so the old queue's residue does not drain on its own and something has to delete it; and **a separately named queue alone does not isolate the generations**, since a `direct` exchange delivers each publish to every queue bound with the matching routing key. The second is a design question for this row's proposal to answer, not one settled here. `docker-compose.yml` gains a `worker` service and the `Dockerfile` a second binary. Reconciles `ddd-architecture`'s Phase 1-era “Phase 6 introduces `POST /jobs` as the canonical async endpoint” scenario against the `POST /api/video-jobs` endpoint that already exists, as a MODIFIED delta rather than a silent implementation choice. The full-flow non-regression (upload → poll → download) that same spec requires is a merge gate here, not optional diligence. **Breaking:** `POST /upload` no longer returns a finished result. | `add-videojob-source-key-and-outbox-relay` | not-started |
| `add-worker-job-lock` | Redis-backed distributed **lease** around worker job pickup, closing the fourth Redis responsibility `add-redis-infrastructure` explicitly deferred from Phase 4 to this phase (`ddd-architecture`'s “Distributed worker lock is deferred, not dropped” scenario). It is a liveness mechanism, not the correctness one — the row above already made the claim atomic in PostgreSQL, and a duplicate delivery simply loses that race. What a conditional update cannot do is tell a job someone is actively working on from one abandoned by a worker that died mid-`ffmpeg`: both read `processing`, and the second is stuck there forever while its redelivery is dead-lettered rather than allowed to recover it. A lease with an expiry is what distinguishes them — but the lease alone does not finish the job, and the row must not stop there: the claim predicate the cutover installed (`status = 'queued'`) still rejects a redelivery for a `processing` row, so an expired lease changes nothing until the takeover is itself an atomic transition. This change therefore widens that predicate to admit a `processing` row whose lease has lapsed, and adds fencing so a worker whose lease expired mid-`ffmpeg` cannot later write `CompleteJob`/`FailJob` over the new holder's work. That `processing → processing` re-entry is a self-transition the canonical state machine does not currently have, so it needs a MODIFIED delta against `ddd-architecture` rather than an implementation-level reading of “backwards transitions are rejected”. That framing also settles the failure posture instead of leaving it open: the lease fails open, like every other Redis-backed feature here, because losing it costs duplicated `ffmpeg` work while the PostgreSQL claim still prevents state corruption — degradation, not a correctness hole. | `migrate-upload-to-async-processing` | not-started |

### Phases 7–8 — not yet decomposed

Listed at phase granularity only. Decomposing these into concrete changes now would mean designing Notification/Observability details that haven't been discussed yet — that happens when each phase is actually next up, using the same discipline as above.

### OpenSpec process itself (workflow change, docs only)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `require-explore-before-propose` | Make `/opsx:explore` required before `/opsx:propose` for Change Backlog rows that are complex or ambiguous (cross-cutting impact, a new architectural pattern/dependency, security/performance/migration complexity, or open design questions) — a narrower distinction than, and layered on top of, the existing trivial-edit exemption that skips OpenSpec entirely. Simple, already-scoped rows may still go straight to `/opsx:propose`. Updates `CLAUDE.md`'s workflow description and this file's "How to use this" guidance, and adds a `development-workflow` spec requirement capturing the rule. Config/docs only, no application code. | none | [archived](../openspec/changes/archive/2026-08-09-require-explore-before-propose/), promoted into [`openspec/specs/development-workflow/spec.md`](../openspec/specs/development-workflow/spec.md) |

## Current State (Phases 1–4 done)

Phase 1 established the architectural foundation — bounded contexts, aggregate design, ADRs, dependency rules, and this documentation — without touching any application code.

Phase 2 added the first executable bounded context: `internal/identity/{domain,application,infrastructure}`, PostgreSQL-backed user registration and login, JWT bearer authentication, and per-artifact ownership enforcement for uploads/downloads/status. Identity configuration (`IDENTITY_POSTGRES_DSN`/`IDENTITY_JWT_SIGNING_KEY`) is mandatory at startup — the server fails to start rather than falling back to unauthenticated video processing (see `enforce-mandatory-identity-config` in the Change Backlog above, correcting the opt-in behavior Phase 2 originally shipped with). The composition root is now `cmd/api` (Phase 3's `extract-cmd-api-entrypoint`, see the Change Backlog above), not the repo-root `main.go` Phase 2 originally wired into. See [docs/architecture.md](architecture.md) for the current request flow and [docs/operations.md](operations.md) for the identity configuration.

Phase 3 is done. `internal/video/domain` and `application` are fully built: `CreateVideoJob`/`GetJobStatus`/`ListUserJobs` plus the state-transition use cases `EnqueueVideoJob`/`StartProcessing`/`CompleteJob`/`FailJob` and a `ProcessVideoJob` orchestration use case, all wired into `cmd/api` (`VIDEO_POSTGRES_DSN` is mandatory at startup, alongside the `IDENTITY_*` variables). Two HTTP-reachable paths use this layer: `POST /api/video-jobs`/`GET /api/video-jobs/:id`/`GET /api/video-jobs` (a preview job-lifecycle API, wired by `wire-videojob-http-endpoints`) and the legacy `POST /upload` (its `ffmpeg` call migrated into this application layer by `migrate-ffmpeg-execution-to-videojob-application`, still fully synchronous — no queue/worker until Phase 6). `POST /api/video-jobs` still gives a created job no processing trigger — `migrate-ffmpeg-execution-to-videojob-application` drives `EnqueueVideoJob`/`StartProcessing`/`CompleteJob`/`FailJob` from `/upload`, not from that endpoint — so a job created directly via `POST /api/video-jobs` still stays `pending` forever; giving it its own trigger would need a separate, not-yet-proposed future change. Because `/upload` and `/api/video-jobs` share the same `VideoJob` repository, `GET /api/video-jobs`/`GET /api/video-jobs/:id` do show real `completed`/`failed` jobs for a user who has used `/upload`. The frontend keeps using `POST /upload` exclusively; it does not consume `/api/video-jobs`.

Phase 4 is done: `internal/platform/redis` (`add-redis-infrastructure`) provides connection plumbing, and all three of its planned features are wired in — idempotency keys on `POST /upload` (`add-upload-idempotency-keys`), per-user rate limiting on every authenticated route (`add-rate-limiting-middleware`), and a cache-aside/write-through status cache for `VideoJob` lookups (`add-videojob-status-cache`), decorating the PostgreSQL repository ahead of `GetJobStatus`'s repeated `GET /api/video-jobs/:id` polling reads. PostgreSQL remains authoritative on any cache miss. A design correction to the idempotency feature (`fail-open-upload-idempotency`, see Change Backlog) has also shipped: a `Reserve` error now fails open, logging the error and proceeding without dedup protection, matching rate limiting's and the status cache's posture — none of Phase 4's Redis-backed features can block the critical path (video processing) on a Redis outage anymore, though what each one degrades to differs: idempotency and the status cache lose an optimization (bounded extra `ffmpeg`/PostgreSQL cost, no correctness impact), while rate limiting loses its abuse-control enforcement entirely for the outage's duration (requests that should have been rejected are allowed through), not just efficiency.

Phase 5 is complete, and object storage is now load-bearing for every artifact. `add-minio-infrastructure` provided the connection adapter; `migrate-result-storage-to-minio` wired it into `cmd/api` for results; `migrate-upload-storage-to-minio` did the same for uploaded source videos; and `add-presigned-download-urls` took the API out of the result-byte path entirely — `GET /download/:filename` authorizes and issues a five-minute signed URL, and the client fetches the ZIP from MinIO directly. Both `outputs/` and `uploads/` are gone, along with the `.owner` sidecar mechanism in its entirety — artifact ownership comes from the `VideoJob` row, and source objects are never served at all. `temp/` is the only local directory left, holding per-request scratch that is always cleaned up. The four required `VIDEO_MINIO_*` variables are a fail-closed startup precondition: the server refuses to start rather than accepting an upload it cannot store; `VIDEO_MINIO_PUBLIC_ENDPOINT`/`VIDEO_MINIO_PUBLIC_USE_SSL` joined them as optional variables naming the address clients reach the bucket at, which matters because SigV4 signs `Host` and a signed URL's host cannot be corrected after issuance. `ProcessVideoJob` now takes a source storage key rather than a local filesystem path, which is the seam Phase 6's `cmd/worker` depends on — it will not share a filesystem with `cmd/api`. Two consequences of presigned delivery are accepted rather than mitigated and are documented in `docs/operations.md`: an issued URL cannot be revoked before it expires, and result bytes are no longer rate limited, since the transfer never passes through the API. Phases 6–8 remain planned. All of them are sequenced by dependency: each phase safely assumes the prior phase's code is merged. The phases do not need to be fully completed to deliver value — even partial implementation (e.g., through Phase 3 or 4) produces a meaningfully more structured and capable system.
