# Roadmap

FIAP X evolves from the current synchronous monolith (`main.go`) into a fully structured DDD system across exactly **8 phases**. Each phase decomposes into one or more focused OpenSpec changes — see [Change Backlog](#change-backlog) below for the concrete, right-sized units and their order. A phase is a grouping for human understanding, not a promise that it ships as a single change.

> **Canonical source:** The authoritative roadmap — including full scope descriptions, ADRs, dependency rules, and the complete domain model — lives in [`openspec/specs/ddd-architecture/spec.md`](../openspec/specs/ddd-architecture/spec.md). Phase 2's own scope is likewise authoritative in [`openspec/specs/identity-authentication/spec.md`](../openspec/specs/identity-authentication/spec.md).
>
> This file is a summary for human readers. If the Phase Summary or any architecture/target-state description below conflicts with the canonical source, the canonical source takes precedence.
>
> The **Change Backlog** section below is different: it's a planning/sequencing artifact with no counterpart in `openspec/specs/` — canonical specs describe target system behavior, not the order in which OpenSpec changes get proposed. It can't "conflict" with the canonical source in the same sense; it's authoritative for change sequencing in its own right. See [`openspec/specs/development-workflow/spec.md`](../openspec/specs/development-workflow/spec.md) for how it's maintained.

## Phase Summary

| Phase | Scope | Status |
|---|---|---|
| **1** | OpenSpec artifacts only: bounded contexts, `VideoJob` aggregate, package topology, ADRs, dependency rules, frontend/presentation layer contract, permanent project documentation. No application code changes. Shipped as a single change, `establish-ddd-architecture-foundation`. | Done |
| **2** | `internal/identity/domain` and `application`; JWT bearer middleware; `RegisterUser` and `AuthenticateUser` use cases; PostgreSQL adapter for Identity; explicit per-artifact ownership enforcement. Wired into the existing `main.go` composition root rather than a new `cmd/api` — that migration stays in Phase 3. Shipped as a single change, `implement-identity-authentication-from-scratch`; a design correction is now tracked separately (see Change Backlog). | Done, correction pending |
| **3** | `internal/video/domain` and `application`; PostgreSQL schema for `video_jobs` and `outbox`; `CreateVideoJob` and `GetJobStatus` use cases; synchronous `ffmpeg` call migrated from `main.go` into application layer; frontend extracted from `getHTMLForm()` into `web/index.html`, `web/styles.css`, `web/app.js`. Decomposed into multiple changes — see Change Backlog. | Done |
| **4** | Redis adapter (`internal/platform/redis` — shipped, see Change Backlog); idempotency keys on `POST /upload` (shipped, see Change Backlog); rate-limiting middleware (shipped, see Change Backlog); status cache for `GetJobStatus` (shipped, see Change Backlog). PostgreSQL remains source of truth. A design correction to the idempotency feature's Redis-error handling has also shipped (`fail-open-upload-idempotency`, see Change Backlog). | Done |
| **5** | MinIO storage adapter behind the `StoragePort` interface; migrate upload and result storage from local filesystem to MinIO; presigned download URLs. Decomposed into four changes — see Change Backlog. | Planned |
| **6** | RabbitMQ infrastructure; `EnqueueVideoJob` publishes to queue; transactional outbox relay; `cmd/worker` picks up messages and runs `ffmpeg`; distributed lock (Redis-backed) preventing concurrent worker pickup of the same job; `POST /upload` becomes non-blocking (returns job ID immediately); `web/app.js` updated for async polling flow. Not yet decomposed. | Planned |
| **7** | `internal/notification/domain` and `application`; RabbitMQ event subscription; email and webhook delivery; `NotificationPreference` per user; retry logic and HMAC webhook signatures. Not yet decomposed. | Planned |
| **8** | Structured logging; Prometheus metrics; `/health` and `/ready` endpoints; `docker-compose.yml` for full local dev stack (PostgreSQL, Redis, RabbitMQ, MinIO). Not yet decomposed. Dockerfile hardening was originally planned here but has been pulled forward — see `harden-dockerfile` in the Change Backlog. | Planned |

## Change Backlog

This is the single source of truth for the product/architecture-scope OpenSpec work ahead — the 8-phase DDD roadmap above, decomposed to the finer-grained `openspec/changes/<name>/` level. For which changes get a row here, when this document is touched, and the rest of the OpenSpec/PR process, see [`openspec/specs/development-workflow/spec.md`](../openspec/specs/development-workflow/spec.md).

### Phase 2 corrections (already shipped — fixing a design mistake)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `enforce-mandatory-identity-config` | Remove the "runs unauthenticated when entirely unconfigured" path (`setupIdentity`'s current behavior). Startup must fail without both `IDENTITY_POSTGRES_DSN` and `IDENTITY_JWT_SIGNING_KEY`. Every protected route always requires a valid bearer token — no bypass. Corrects the "entirely unconfigured runs video-processing only" scenario in `openspec/specs/identity-authentication/spec.md`. | none | [archived](../openspec/changes/archive/2026-08-10-enforce-mandatory-identity-config/), promoted into [`openspec/specs/identity-authentication/spec.md`](../openspec/specs/identity-authentication/spec.md) |

### Frontend (orthogonal to everything else, low risk)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `extract-frontend-to-static-files` | Move `getHTMLForm()`'s HTML/CSS/JS out of `main.go` into `web/index.html`, `web/app.js`, `web/styles.css`, served via `go:embed`. No behavior change. | none | [archived](../openspec/changes/archive/2026-08-11-extract-frontend-to-static-files/), promoted into [`openspec/specs/ddd-architecture/spec.md`](../openspec/specs/ddd-architecture/spec.md) |

### Local development tooling (orthogonal to everything else, low risk)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `add-docker-compose-app-service` | Add an `app` service to `docker-compose.yml` alongside the existing `postgres` service: builds the existing Dockerfile, wires `IDENTITY_POSTGRES_DSN`/`IDENTITY_JWT_SIGNING_KEY` over the compose network, gated on Postgres's healthcheck so it doesn't hit the fail-fast unreachable-DB error before Postgres is ready, bind-mounts `uploads/`/`outputs/` to the host, publishes the port loopback-only. `docker-compose.yml` becomes the **sole** documented Docker workflow — the plain `docker build`/`docker run` path and the separate Docker-based test fallback are retired from documentation, not kept alongside it (`docs/operations.md`'s deployment-focused Docker section is the one exception, explicitly out of scope). A separate `identity_test` database (via a postgres init script) keeps the test command from truncating the running app's data. Config/docs only, no application code. | none | [archived](../openspec/changes/archive/2026-08-08-add-docker-compose-app-service/), promoted into [`openspec/specs/development-workflow/spec.md`](../openspec/specs/development-workflow/spec.md) |

### Dockerfile hardening (pulled forward from Phase 8, orthogonal, low risk)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `harden-dockerfile` | Replaced the single-stage, root-user `Dockerfile` (documented anti-pattern; ran `go mod tidy` at build time against whatever the module proxy currently serves, rather than resolving deterministically from the committed `go.sum`, and shipped the full Go toolchain in the final image since there was no separate runtime stage) with a three-stage build: `builder` (read-only `go.sum`-verified deps, static binary), `test` (`builder` + `ffmpeg`, backs the new `docker-compose.yml` `app-test` service), and `runtime` (pinned `alpine:3.24`, `ffmpeg`, non-root UID 1000, no Go toolchain). Pulled forward from Phase 8 at the user's explicit request — no longer bundled with that phase's observability work. | `add-docker-compose-app-service` | [archived](../openspec/changes/archive/2026-08-09-harden-dockerfile/), promoted into [`openspec/specs/container-image/spec.md`](../openspec/specs/container-image/spec.md) and [`openspec/specs/development-workflow/spec.md`](../openspec/specs/development-workflow/spec.md) |

### Phase 3 — Video Processing persistence & `cmd/api` extraction

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `add-videojob-domain-and-application` | `VideoJob` aggregate, state machine, value objects, repository/port interfaces, and exactly three use cases: `CreateVideoJob`, `GetJobStatus`, `ListUserJobs`. No infra, no HTTP. None of the three use cases in scope trigger a state transition (`CreateVideoJob` only creates in `pending`), so `EnqueueVideoJob`, `StartProcessing`, `CompleteJob`, and `FailJob` are all out of scope here. Per the canonical state machine (`openspec/specs/ddd-architecture/spec.md`), `CompleteJob`/`FailJob` are only valid from `processing`, which is reachable solely via `EnqueueVideoJob` then `StartProcessing` — so all four transition use cases are added together by `migrate-ffmpeg-execution-to-videojob-application` below, calling all four in sequence synchronously in-process (no real queue/worker yet). Phase 6 later replaces that in-process sequencing with an actual RabbitMQ-backed queue and a separate `cmd/worker`, not with new transition logic. | none | [archived](../openspec/changes/archive/2026-08-16-add-videojob-domain-and-application/), promoted into [`openspec/specs/ddd-architecture/spec.md`](../openspec/specs/ddd-architecture/spec.md) and [`openspec/specs/videojob-lifecycle/spec.md`](../openspec/specs/videojob-lifecycle/spec.md) |
| `add-videojob-infrastructure` | PostgreSQL adapter for `VideoJob`, jobs + outbox schema/migration. | `add-videojob-domain-and-application` | [archived](../openspec/changes/archive/2026-08-16-add-videojob-infrastructure/), promoted into [videojob-persistence spec](../openspec/specs/videojob-persistence/spec.md) |
| `extract-cmd-api-entrypoint` | Move the HTTP composition root from `main.go` into `cmd/api`, preserving current routes/behavior 1:1. | `extract-frontend-to-static-files` (no Go string literal to drag along) | [archived](../openspec/changes/archive/2026-08-16-extract-cmd-api-entrypoint/), promoted into [`openspec/specs/ddd-architecture/spec.md`](../openspec/specs/ddd-architecture/spec.md) |
| `wire-videojob-http-endpoints` | New job-oriented endpoints backed by `VideoJob`, added alongside the legacy synchronous flow (not yet replacing it — this row only wires the new read/create paths). Note: jobs created through these new endpoints have no processing trigger through the rest of Phase 3 either — `migrate-ffmpeg-execution-to-videojob-application` below only migrates the legacy `POST /upload` handler's `ffmpeg` call, not this row's new create endpoint, so a job created here stays stuck in `pending` even after that row ships. Acceptable only because the frontend keeps using the legacy `/upload` flow throughout Phase 3, so it doesn't affect real usage — but the new endpoints must not be presented or documented as a working end-to-end path until a later change explicitly wires their processing trigger too. | `add-videojob-infrastructure`, `extract-cmd-api-entrypoint` | [archived](../openspec/changes/archive/2026-08-17-wire-videojob-http-endpoints/), promoted into [videojob-http-api spec](../openspec/specs/videojob-http-api/spec.md) |
| `migrate-ffmpeg-execution-to-videojob-application` | Cut `POST /upload`'s `ffmpeg` invocation over to run through the `VideoJob` application layer (still synchronous — no queue/worker until Phase 6) and retire the legacy in-`main.go`/`cmd/api` exec path. Adds `EnqueueVideoJob`, `StartProcessing`, `CompleteJob`, and `FailJob` to `internal/video/application` (all deferred by `add-videojob-domain-and-application` above since nothing called them yet), calling all four synchronously in-process — `pending → queued → processing → completed`/`failed` — around the `ffmpeg` invocation, satisfying the canonical state machine's requirement that completion/failure only follow `processing`. This is what actually fulfills Phase 3's "synchronous `ffmpeg` call migrated from `main.go` into application layer" promise; without this row the prior four only add a parallel path and never complete Phase 3. Known consequence, not a defect: `/upload` now creates real `VideoJob` rows, so `GET /api/video-jobs`/`GET /api/video-jobs/:id` start showing `completed`/`failed` jobs for a user who has used `/upload`, alongside any still-`pending` jobs created directly via `POST /api/video-jobs` (whose own "no processing trigger" guarantee is unaffected — this row does not touch that endpoint). | `wire-videojob-http-endpoints` | [archived](../openspec/changes/archive/2026-08-17-migrate-ffmpeg-execution-to-videojob-application/), promoted into [videojob-lifecycle](../openspec/specs/videojob-lifecycle/spec.md), [videojob-execution](../openspec/specs/videojob-execution/spec.md), [videojob-persistence](../openspec/specs/videojob-persistence/spec.md), and [videojob-http-api](../openspec/specs/videojob-http-api/spec.md) specs |

### Phase 4 — Redis infrastructure & performance/reliability features

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `add-redis-infrastructure` | `internal/platform/redis`: `Config`/`LoadConfigFromEnv` (reads `REDIS_ADDR`), `Open` (bare client construction, no connectivity check), `Ping` (health check), `Close`. New `internal/platform/` namespace for cross-cutting infrastructure owned by no single bounded context. `docker-compose.yml`/CI gain a `redis` service, consumed only by this package's own tests. No `cmd/api`/`cmd/worker` wiring — the three Phase 4 features (idempotency keys, rate limiting, status cache) are separate, not-yet-proposed changes that consume this adapter. Also rescoped `ddd-architecture`'s "Redis Responsibilities" requirement to three Phase 4 responsibilities, deferring the fourth (distributed worker lock) to Phase 6. | none | [archived](../openspec/changes/archive/2026-08-18-add-redis-infrastructure/), promoted into [`openspec/specs/redis-infrastructure/spec.md`](../openspec/specs/redis-infrastructure/spec.md) and [`openspec/specs/ddd-architecture/spec.md`](../openspec/specs/ddd-architecture/spec.md) |
| `add-upload-idempotency-keys` | Content-hash idempotency for `POST /upload`: `IdempotencyKey` value object + `IdempotencyStore` port (`internal/video/domain`), `RedisStore` adapter (`internal/video/infrastructure/idempotency`) backed by `internal/platform/redis`. `handleVideoUpload` hashes the upload (SHA-256) while saving it, reserves a key scoped to `UserID` + content hash before calling `CreateVideoJob`, and a losing duplicate request polls for the winner's job status (bounded retry) instead of running `ffmpeg` again. `REDIS_ADDR` becomes required at `cmd/api` startup. First of Phase 4's three features to ship. | `add-redis-infrastructure` | [archived](../openspec/changes/archive/2026-08-21-add-upload-idempotency-keys/), promoted into [`openspec/specs/upload-idempotency/spec.md`](../openspec/specs/upload-idempotency/spec.md) |
| `add-rate-limiting-middleware` | Per-authenticated-user request rate limiting on every route gated by `identity.requireBearerAuth()`: a Redis-backed fixed-window `Limiter` (`internal/platform/ratelimit`, atomic `INCR`+conditional-`EXPIRE` via a Lua script, mirroring `idempotency.RedisStore`'s atomicity pattern) mounted as `cmd/api/ratelimit.go`'s `rateLimitMiddleware` on the `videoRoutes` group, immediately after the auth middleware. Excess requests get `429` + `Retry-After` (derived from Redis `PTTL`, rounded up to whole seconds) before any handler runs; a limiter failure — or an internal bounded per-call timeout firing — fails open rather than blocking otherwise-healthy traffic. Thresholds (`RATE_LIMIT_MAX_REQUESTS`/`RATE_LIMIT_WINDOW_SECONDS`, defaults 60/60s) are optional, unlike `REDIS_ADDR`. Second of Phase 4's three features to ship. | `add-redis-infrastructure` | [archived](../openspec/changes/archive/2026-08-21-add-rate-limiting-middleware/), promoted into [`openspec/specs/rate-limiting/spec.md`](../openspec/specs/rate-limiting/spec.md) |
| `add-videojob-status-cache` | Cache-aside/write-through Redis cache for `VideoJob` lookups: `CachedVideoJobRepository` (`internal/video/infrastructure/cache`) decorates the PostgreSQL `VideoJobRepository`, wired into `cmd/api/video.go`'s `setupVideo` ahead of every use case (`GetJobStatus`'s repeated `GET /api/video-jobs/:id` polling reads are the main beneficiary). `FindByID` reads cache-aside (a hit skips PostgreSQL; a miss/error/corrupted entry always falls back to PostgreSQL unconditionally, then attempts repopulation via `SET NX`, never a plain `SET`, so a slow reader can't clobber a fresher write-through value — a race caught in review). Repopulation is guaranteed on a true miss or a malformed-but-string entry (self-healed via a compare-and-delete Lua script first) but only best-effort on a generic Redis error (e.g. `WRONGTYPE`), since `SET NX` can't replace an existing key of the wrong type — PostgreSQL correctness is unaffected either way. `Update` writes through with an unconditional `SET` after the PostgreSQL write succeeds. Fixed 5-minute TTL, not configurable — no new environment variable. `FindByUserID`/`Create` are intentionally uncached. Third and final of Phase 4's three features to ship — Phase 4 is now complete. | `add-redis-infrastructure` | [archived](../openspec/changes/archive/2026-08-21-add-videojob-status-cache/), promoted into [`openspec/specs/videojob-status-cache/spec.md`](../openspec/specs/videojob-status-cache/spec.md) |

### Phase 4 corrections (already shipped — fixing a design mistake)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `fail-open-upload-idempotency` | `POST /upload`'s idempotency reservation (`RedisStore.Reserve`) previously failed closed on a Redis error — the request returned `500` instead of proceeding. Corrects this to fail open, matching rate limiting and the status cache: a `Reserve` error is logged and the request proceeds to `CreateVideoJob` without dedup protection, so a Redis outage degrades an optimization instead of blocking the critical path (video processing) — decisive given this system is being prepared to run multiple `cmd/api` replicas, where fail-closed would otherwise become a fleet-wide synchronized outage. Modified the existing "Concurrent Identical Requests Are Serialized" requirement in `openspec/specs/upload-idempotency/spec.md`. | `add-upload-idempotency-keys` | [archived](../openspec/changes/archive/2026-08-22-fail-open-upload-idempotency/), promoted into [`openspec/specs/upload-idempotency/spec.md`](../openspec/specs/upload-idempotency/spec.md) |

### Phase 5 — Object storage migration

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `add-minio-infrastructure` | `internal/video/infrastructure/storage`: `Config`/`LoadConfigFromEnv` (reads `VIDEO_MINIO_ENDPOINT`, `VIDEO_MINIO_ACCESS_KEY`, `VIDEO_MINIO_SECRET_KEY`, `VIDEO_MINIO_BUCKET`, optional `VIDEO_MINIO_USE_SSL`), `Open` (client construction, no connectivity check, but an `error` return unlike `redis.Open` — the underlying constructor parses the endpoint and builds the transport; it does **not** validate credentials), `Ping` (a real `BucketExists` round trip, not `minio-go`'s cached `IsOnline`), and `EnsureBucket` (idempotent, concurrency-safe). No `Close`: `*minio.Client` exposes no teardown and keeps its transport unexported, so a wrapper could only return `nil` while releasing nothing. The package sits in the Video Processing context rather than `internal/platform/`, following the PostgreSQL precedent — `internal/identity/infrastructure/postgres` and `internal/video/infrastructure/postgres` are separate context-owned packages against one physical database — since every MinIO consumer is Video Processing; it therefore modifies no canonical spec. `docker-compose.yml`/CI gain a `minio` service, consumed only by this package's own tests; unlike `redis`, it gets a named volume, since from the next change onward the bucket holds the only durable copy of a completed job's zip. CI starts it via `docker run` rather than a `services:` entry — verified against the pinned image, whose `CMD` lacks `server /data`, which a service container cannot supply. No `cmd/api` wiring and no new required startup configuration. | none | not-started |
| `migrate-result-storage-to-minio` | Add `StoragePort` (`internal/video/domain`) and its adapter (`internal/video/infrastructure/storage`); the frames zip is uploaded to MinIO instead of written to `outputs/`, and `GET /download/:filename`/`GET /api/status` are re-sourced accordingly. Retires `outputs/` and its `.owner` sidecars, deriving artifact ownership from the `VideoJob` row instead. Scope only — the design (including how `/api/status` replaces its `outputs/*.zip` glob) lands in this change's own proposal. | `add-minio-infrastructure` | not-started |
| `migrate-upload-storage-to-minio` | `POST /upload` stores the uploaded video in MinIO rather than `uploads/`, and frame extraction obtains a local copy for `ffmpeg`, which needs a filesystem path. Retires `uploads/` and its `.owner` sidecars. Scope only — design in this change's own proposal. | `migrate-result-storage-to-minio` | not-started |
| `add-presigned-download-urls` | Result downloads are served as bounded-TTL presigned URLs rather than proxied through the API, with ownership enforced at issuance since a presigned URL bypasses bearer auth once issued. Adds the corresponding requirement to `video-processing-access`. Scope only — design in this change's own proposal. | `migrate-result-storage-to-minio` | not-started |

### Phases 6–8 — not yet decomposed

Listed at phase granularity only. Decomposing these into concrete changes now would mean designing RabbitMQ/Notification/Observability details that haven't been discussed yet — that happens when each phase is actually next up, using the same discipline as above.

### OpenSpec process itself (workflow change, docs only)

| Change | Scope | Depends on | Status |
|---|---|---|---|
| `require-explore-before-propose` | Make `/opsx:explore` required before `/opsx:propose` for Change Backlog rows that are complex or ambiguous (cross-cutting impact, a new architectural pattern/dependency, security/performance/migration complexity, or open design questions) — a narrower distinction than, and layered on top of, the existing trivial-edit exemption that skips OpenSpec entirely. Simple, already-scoped rows may still go straight to `/opsx:propose`. Updates `CLAUDE.md`'s workflow description and this file's "How to use this" guidance, and adds a `development-workflow` spec requirement capturing the rule. Config/docs only, no application code. | none | [archived](../openspec/changes/archive/2026-08-09-require-explore-before-propose/), promoted into [`openspec/specs/development-workflow/spec.md`](../openspec/specs/development-workflow/spec.md) |

## Current State (Phases 1–4 done)

Phase 1 established the architectural foundation — bounded contexts, aggregate design, ADRs, dependency rules, and this documentation — without touching any application code.

Phase 2 added the first executable bounded context: `internal/identity/{domain,application,infrastructure}`, PostgreSQL-backed user registration and login, JWT bearer authentication, and per-artifact ownership enforcement for uploads/downloads/status. Identity configuration (`IDENTITY_POSTGRES_DSN`/`IDENTITY_JWT_SIGNING_KEY`) is mandatory at startup — the server fails to start rather than falling back to unauthenticated video processing (see `enforce-mandatory-identity-config` in the Change Backlog above, correcting the opt-in behavior Phase 2 originally shipped with). The composition root is now `cmd/api` (Phase 3's `extract-cmd-api-entrypoint`, see the Change Backlog above), not the repo-root `main.go` Phase 2 originally wired into. See [docs/architecture.md](architecture.md) for the current request flow and [docs/operations.md](operations.md) for the identity configuration.

Phase 3 is done. `internal/video/domain` and `application` are fully built: `CreateVideoJob`/`GetJobStatus`/`ListUserJobs` plus the state-transition use cases `EnqueueVideoJob`/`StartProcessing`/`CompleteJob`/`FailJob` and a `ProcessVideoJob` orchestration use case, all wired into `cmd/api` (`VIDEO_POSTGRES_DSN` is mandatory at startup, alongside the `IDENTITY_*` variables). Two HTTP-reachable paths use this layer: `POST /api/video-jobs`/`GET /api/video-jobs/:id`/`GET /api/video-jobs` (a preview job-lifecycle API, wired by `wire-videojob-http-endpoints`) and the legacy `POST /upload` (its `ffmpeg` call migrated into this application layer by `migrate-ffmpeg-execution-to-videojob-application`, still fully synchronous — no queue/worker until Phase 6). `POST /api/video-jobs` still gives a created job no processing trigger — `migrate-ffmpeg-execution-to-videojob-application` drives `EnqueueVideoJob`/`StartProcessing`/`CompleteJob`/`FailJob` from `/upload`, not from that endpoint — so a job created directly via `POST /api/video-jobs` still stays `pending` forever; giving it its own trigger would need a separate, not-yet-proposed future change. Because `/upload` and `/api/video-jobs` share the same `VideoJob` repository, `GET /api/video-jobs`/`GET /api/video-jobs/:id` do show real `completed`/`failed` jobs for a user who has used `/upload`. The frontend keeps using `POST /upload` exclusively; it does not consume `/api/video-jobs`.

Phase 4 is done: `internal/platform/redis` (`add-redis-infrastructure`) provides connection plumbing, and all three of its planned features are wired in — idempotency keys on `POST /upload` (`add-upload-idempotency-keys`), per-user rate limiting on every authenticated route (`add-rate-limiting-middleware`), and a cache-aside/write-through status cache for `VideoJob` lookups (`add-videojob-status-cache`), decorating the PostgreSQL repository ahead of `GetJobStatus`'s repeated `GET /api/video-jobs/:id` polling reads. PostgreSQL remains authoritative on any cache miss. A design correction to the idempotency feature (`fail-open-upload-idempotency`, see Change Backlog) has also shipped: a `Reserve` error now fails open, logging the error and proceeding without dedup protection, matching rate limiting's and the status cache's posture — none of Phase 4's Redis-backed features can block the critical path (video processing) on a Redis outage anymore, though what each one degrades to differs: idempotency and the status cache lose an optimization (bounded extra `ffmpeg`/PostgreSQL cost, no correctness impact), while rate limiting loses its abuse-control enforcement entirely for the outage's duration (requests that should have been rejected are allowed through), not just efficiency.

Phase 5 is decomposed into the four changes listed in the Change Backlog above; none of them are implemented yet. Phases 6–8 remain planned. All of them are sequenced by dependency: each phase safely assumes the prior phase's code is merged. The phases do not need to be fully completed to deliver value — even partial implementation (e.g., through Phase 3 or 4) produces a meaningfully more structured and capable system.
