*:Zone.Identifier

/uploads/*
/temp/*
!/uploads/.gitkeep
!/temp/.gitkeep
# Retired by migrate-result-storage-to-minio; still ignored so a
# pre-migration leftover can't be committed by accident.
/outputs/

*.exe
/app
/video-processor

.claude/worktrees
__pycache__/
