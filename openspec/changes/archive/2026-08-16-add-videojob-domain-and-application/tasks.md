## 1. Implementation (implementation PR)

- [x] 1.1 Create `internal/video/domain/user_id.go`: a `UserID` value object local to this context (distinct Go type from `internal/identity/domain.UserID`) with a validating constructor (`NewUserID(string) (UserID, error)`, non-empty invariant only), `String()`, `IsZero()`, `Equal()`. No generator, no parser port — Video Processing only ever receives an already-verified ID string.
- [x] 1.2 Create `internal/video/domain/video_job_id.go`: `VideoJobID` value object mirroring `internal/identity/domain/user_id.go`'s shape — `NewVideoJobID(string)` (non-empty invariant), `VideoJobIDGenerator` port (mints new IDs), `VideoJobIDParser` port (validates/parses an externally-supplied ID string, returning a distinct error for malformed input — see 1.10).
- [x] 1.3 Create `internal/video/domain/job_status.go`: `JobStatus` enum covering `pending`, `queued`, `processing`, `completed`, `failed`, plus a pure `CanTransitionTo(next JobStatus) bool` (or equivalent) encoding the valid-transitions table (`pending → queued → processing → completed`, `processing → failed`; no backwards transitions; no skipping states). No `VideoJob` mutator methods use this yet — it stands alone, independently unit-tested.
- [x] 1.4 Create `internal/video/domain/original_filename.go`: `OriginalFilename` value object — non-empty **and** extension in the supported-video set (`.mp4`, `.avi`, `.mov`, `.mkv`, `.wmv`, `.flv`, `.webm`, matching `main.go`'s `isValidVideoFile`).
- [x] 1.5 Create `internal/video/domain/storage_key.go`: `StorageKey` value object, non-empty invariant (only set once a job reaches `completed`; unset otherwise).
- [x] 1.6 Create `internal/video/domain/video_job.go`: `VideoJob` aggregate root with fields for `VideoJobID`, `UserID`, `OriginalFilename`, `StorageKey`, `FrameCount` (int, ≥0, aggregate-validated primitive — not a standalone value object, see `design.md`), `ErrorReason` (string, empty unless `failed`, same reasoning), `JobStatus`, and `CreatedAt` (`time.Time`, with an accessor — this is `ListUserJobs`' sort key, see 1.11/1.13). `NewVideoJob(generator VideoJobIDGenerator, userID UserID, filename OriginalFilename, createdAt time.Time) (*VideoJob, error)` always produces `JobStatus: pending`, `FrameCount: 0`, empty `ErrorReason`, unset `StorageKey`. `RestoreVideoJob(...)` reconstructs from already-validated storage values, including `CreatedAt`. Accessors only — no `Enqueue`/`StartProcessing`/`Complete`/`Fail` methods (out of scope; see `design.md`).
- [x] 1.7 Create `internal/video/domain/repository.go`: `VideoJobRepository` port — `Create(ctx, *VideoJob) error`, `FindByID(ctx, VideoJobID) (*VideoJob, error)`, `FindByUserID(ctx, UserID, offset, limit int) ([]*VideoJob, error)` (results ordered by `CreatedAt` descending, `VideoJobID` as tie-breaker — callers pass already-validated `offset ≥ 0`/`1 ≤ limit ≤ 100`, per 1.11) — plus `ErrVideoJobNotFound`.
- [x] 1.8 Create `internal/video/application/clock.go`: a `Clock` interface (`Now() time.Time`), independent of `internal/identity/application`'s copy — importing another context's `application` package is forbidden by the dependency rules, so this is a small intentional duplication, not an oversight.
- [x] 1.9 Create `internal/video/application/create_video_job.go`: `CreateVideoJob` use case — input `{UserID, OriginalFilename}`, wires `VideoJobIDGenerator`, `VideoJobRepository`, `Clock`; returns a result describing the created (`pending`) job.
- [x] 1.10 Create `internal/video/application/get_job_status.go`: `GetJobStatus` use case — input `{RequestingUserID, JobID string}`; parses `JobID` via `VideoJobIDParser`, returning that parser's error directly (distinct from not-found) on malformed input without querying the repository; otherwise fetches via the repository and returns `ErrVideoJobNotFound` both when the job does not exist and when `RequestingUserID` does not match the job's owner (never a distinct "forbidden" result). The result includes `StorageKey` (empty unless `completed`) alongside `JobStatus`/`FrameCount`/`ErrorReason`.
- [x] 1.11 Create `internal/video/application/list_user_jobs.go`: `ListUserJobs` use case — input `{UserID, Offset, Limit}`; validates `1 ≤ Limit ≤ 100` and `Offset ≥ 0`, returning an error (without querying the repository) if out of range; otherwise delegates to `VideoJobRepository.FindByUserID`.
- [x] 1.12 Create `internal/video/application/fakes_test.go`: in-memory `fakeVideoJobRepository` (must implement the `CreatedAt`-descending/`VideoJobID`-tie-breaker ordering from 1.7 so pagination tests are meaningful), `fakeVideoJobIDGenerator`, `fakeVideoJobIDParser`, `fakeClock`, mirroring `internal/identity/application/fakes_test.go`'s pattern.
- [x] 1.13 Add unit tests for all three use cases (`create_video_job_test.go`, `get_job_status_test.go`, `list_user_jobs_test.go`) covering every scenario in `specs/videojob-lifecycle/spec.md`, including the malformed-ID, completed-job-`StorageKey`, ordering, and out-of-range-pagination scenarios.
- [x] 1.14 Add domain-layer unit tests, mirroring `internal/identity/domain`'s per-type coverage (e.g. `user_test.go`, `email_test.go`, `user_id_test.go`):
  - [x] 1.14.1 `job_status_test.go` — `CanTransitionTo` covering valid, backwards, and skipped-state transitions.
  - [x] 1.14.2 `user_id_test.go` — valid/empty construction for the local `UserID`.
  - [x] 1.14.3 `video_job_id_test.go` — valid/empty construction, generator, and parser (valid and malformed input).
  - [x] 1.14.4 `original_filename_test.go` — valid extensions, empty input, and each rejected/unsupported extension.
  - [x] 1.14.5 `storage_key_test.go` — valid/empty construction.
  - [x] 1.14.6 `video_job_test.go` — `NewVideoJob` produces the expected `pending`/zero/empty initial state and stores `CreatedAt`; `RestoreVideoJob` round-trips all fields including `CreatedAt`.
- [x] 1.15 Create `internal/video/dependency_rules_test.go`, mirroring `internal/identity/dependency_rules_test.go`'s structure but with generic cross-context detection: `domain`/`application` under `internal/video/` must not import `internal/video/infrastructure/`, `net/http`, `database/sql`, `github.com/gin-gonic`, `github.com/golang-jwt`, `github.com/jackc`, **or any `video-processor/internal/<other-context>/...` path** (i.e. any `internal/*` sibling of `internal/video` itself — a generic check, not a hardcoded `internal/identity` entry, so it also catches a future `internal/notification` import without a follow-up edit).

## 2. Verification

- [x] 2.1 `go build ./...` succeeds.
- [x] 2.2 `go test ./... -v` passes (no `ffmpeg` dependency for these packages; existing `main_test.go` behavior is unaffected).
- [x] 2.3 `go vet ./...`, `gosec ./...`, and `govulncheck ./...` report no new findings (all three are required CI gates per `docs/development.md`).
- [x] 2.4 Confirm `internal/video/domain` and `internal/video/application` are not imported anywhere from `main.go` (`grep -rn "internal/video" main.go` returns nothing) — this change must stay inert in the running system.

## 3. Finalization (finalization PR, after implementation merges)

- [x] 3.1 Mark all tasks above complete.
- [x] 3.2 Promote `specs/ddd-architecture/spec.md`'s MODIFIED requirement into `openspec/specs/ddd-architecture/spec.md`.
- [x] 3.3 Promote `specs/videojob-lifecycle/spec.md`'s ADDED requirements into a new `openspec/specs/videojob-lifecycle/spec.md`.
- [x] 3.4 Update `docs/domain-model.md`'s "Cross-Context Contracts" section: remove the "Target: it moves to `pkg/`" note and describe the local-`UserID`-per-context-plus-composition-root-translation model actually implemented.
- [x] 3.5 Update `docs/architecture.md`: remove the `pkg/` entry and its comment from the "Target Package Topology" tree (no `pkg/` directory is introduced by this change or planned by the corrected architecture), and reword Dependency Rule 5 ("Cross-Context communication uses domain events or the shared `UserID` value object from `pkg/`") to describe the local-value-object-plus-composition-root-translation model instead.
- [x] 3.6 Move the change folder to `openspec/changes/archive/`.
- [x] 3.7 Update `docs/roadmap.md`'s `add-videojob-domain-and-application` Change Backlog row to `archived`, linking the archive folder and the promoted specs.
