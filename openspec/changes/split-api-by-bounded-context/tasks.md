# Tasks

Implementation order is incremental and keeps the stack working after every group: the token change lands while `cmd/api` is still whole, then the gateway appears in front of it, then each context's service is extracted from behind the gateway one at a time, with `cmd/api` deleted only when the last one leaves. Each numbered group 1–6 is sized to be one implementation PR. Group 7 is verification and belongs to the last implementation PR; group 8 is finalization and belongs to the change's closure PR.

**Extraction is copy-then-delete, never move.** A composition root is `package main`: a service that leaves cannot import anything from the one it left, and `cmd/api` keeps serving the routes that have not left yet. So each group *writes* the new service's files from the old ones and removes from `cmd/api` only the routes that just moved — `identity.go`'s `requireBearerAuth`/`authenticatedUserID` and `ratelimit.go` stay in `cmd/api` until group 6 deletes the package outright, because the routes still there need them. The same holds for the test suites: every existing `_test.go` under `cmd/api` depends on fixtures declared in the others (`newTestIdentityModuleWithTokens` in `identity_test.go`, `newTestVideoModule` in `video_test.go`, `alwaysAllowRateLimiter` in `video_test.go`, `newNoopNotificationModule` in `notification_test.go`), so a suite is rewritten against its own service's fixtures, not relocated. Anywhere below that reads "move", it means this.

**Prerequisite:** `separate-context-databases` is merged and finalized before group 1 begins. Do not start against a single shared `identity` database.

## 1. Asymmetric access tokens (`cmd/api` still whole)

- [ ] 1.1 Split `internal/identity/infrastructure/jwtauth` into two constructors over the same package: `NewIssuer(privateKeyPEM string, keyID string) (Issuer, error)` parsing a PKCS#8 RSA private key, and `NewVerifier(publicKeysByKeyID map[string]string) (Verifier, error)` parsing PEM public keys. `Issuer` implements `domain.TokenIssuer`; `Verifier` implements `domain.TokenVerifier`. Neither type may satisfy both interfaces — that is the whole point, and it should be a compile error rather than a convention.
- [ ] 1.2 `signingMethod` becomes `jwt.SigningMethodRS256`, and `jwt.WithValidMethods` keeps pinning it by name. Pinning matters more here than it did with HMAC: the public key is not secret, so an unpinned verifier would accept it as an HMAC key and validate a token an attacker signed. Add a test that a token presenting `alg: HS256` signed with the public key's PEM bytes is rejected.
- [ ] 1.3 `Issue` sets the `kid` header from the issuer's key id. `Verify` reads `kid` from the token header, looks it up in the verifier's map, and rejects a token whose `kid` is missing or unknown as `domain.ErrInvalidToken` — the same error every other invalid token returns, so a caller cannot probe which key ids exist.
- [ ] 1.4 `NewVerifier` refuses an empty map and refuses PEM that parses as a *private* key. The refusal is scoped to the PEM handed to `NewVerifier`, not to whether the process holds a private key anywhere: `cmd/identity-api` legitimately has one in its environment and passes it only to `NewIssuer`. What must fail at startup is a service handed the full key pair *as its verification material*, because that service is one line away from being able to mint.
- [ ] 1.5 Replace `IDENTITY_JWT_SIGNING_KEY` with three variables whose split follows the capability split, not the key pair: `IDENTITY_JWT_PRIVATE_KEY` (the issuer's signing key, Identity only), `IDENTITY_JWT_KEY_ID` (which key the issuer signs with, Identity only), and `IDENTITY_JWT_PUBLIC_KEYS` (**a set** of `kid`-to-PEM entries, read by every service that verifies). The set is a set from the first commit because a single pair cannot be rotated: switching the verifier first rejects every token already issued, switching the issuer first mints tokens nothing can verify, and the whole point of `kid` is that both keys are held at once during the overlap. A verifier requires only `IDENTITY_JWT_PUBLIC_KEYS`; it reads `kid` off the token and never needs to be told which key is active.
- [ ] 1.6 Identity requires all three and asserts at startup that `IDENTITY_JWT_PUBLIC_KEYS` contains an entry under `IDENTITY_JWT_KEY_ID` and that it is the private key's match. That check is why Identity reads a public key at all despite registering no bearer-authenticated route, and the reason has to be written down or it will be deleted as dead configuration: a mismatched pair is silent in the one place it could be caught and loud everywhere it cannot be attributed — Identity mints successfully, every other service rejects every token, and the fault appears to belong to the services that are correct.
- [ ] 1.7 Startup fails clearly when any required variable is absent or malformed, when the key set is empty, or when an entry does not parse as a public key. No fallback key of any kind, and no mode in which a missing key degrades to unauthenticated access.
- [ ] 1.8 Generate a fixed development key pair and set the variables on `app` and `app-test` in `docker-compose.yml`, replacing `local-dev-signing-key`. It is local-only material and belongs in the repository for the same reason the compose PostgreSQL password does; name it as such in the compose comment so nobody promotes it.
- [ ] 1.9 Test fixtures: one helper that generates a key pair once per test binary and returns an issuer and a verifier over it. Existing identity tests keep asserting what they assert; only their construction changes. Add a rotation test — a verifier holding two keys accepts tokens signed under either `kid`.
- [ ] 1.10 `go vet ./...` and `go test ./... -v` pass. The whole suite still runs against one `cmd/api`.

## 2. The gateway, in front of the unsplit API

- [ ] 2.1 `docker/nginx/nginx.conf`: one `server` listening on 8080, proxying everything to `app:8080`. No routing decisions yet — this group proves the hop is transparent before anything depends on it being selective.
- [ ] 2.2 Resolve the backend hostname **at request time**, not at configuration load: `resolver 127.0.0.11 valid=10s;` (Docker's embedded DNS) with the upstream address in a variable, `set $backend "app:8080"; proxy_pass http://$backend;`. A literal hostname in `proxy_pass` is resolved once when nginx loads its configuration and cached for the process's life, so recreating a backend container — which gives it a new address — leaves the gateway sending traffic to an address nothing answers on until it is reloaded. That directly contradicts the independent-restart requirement this change exists to establish, and it fails in exactly the way that looks like the backend's fault. The variable form costs a DNS lookup per request against a local resolver.
- [ ] 2.3 `client_max_body_size 0;` (or an explicit bound above the largest upload the stack is expected to accept — not the 1 MB default, which returns `413` before the extension check runs).
- [ ] 2.4 `proxy_request_buffering off;`. The default spools the whole request body to the gateway's disk before opening the upstream connection, which reintroduces the local-disk write `videojob-source-storage` requires `POST /upload` not to make. Leave `proxy_buffering` (responses) at its default — `GET /download/:filename` returns a small JSON body and the bytes go straight from MinIO to the client.
- [ ] 2.5 Set `proxy_read_timeout`/`proxy_send_timeout` above the longest legitimate request. `POST /upload` returns `202` as soon as the job is queued, so the bound is upload transfer time, not `ffmpeg` time.
- [ ] 2.6 Forward `X-Forwarded-For`/`X-Forwarded-Proto`/`Host`. Nothing reads them today; omitting them makes the first thing that does silently wrong.
- [ ] 2.7 `docker-compose.yml`: add a `gateway` service on the stock nginx image, mounting the config read-only, publishing `127.0.0.1:8080:8080`, depending on `app`. Remove `app`'s `ports` block — from here on, only the gateway publishes.
- [ ] 2.8 Verify the full documented flow through the gateway with `cmd/api` unchanged behind it: register, log in, `POST /upload` with a real video, poll `GET /api/video-jobs/:id`, `GET /api/status`, `GET /download/:filename`, and redeem the presigned URL. Verify an upload larger than 1 MB succeeds — that is the assertion 2.3 exists for, and the default would pass every other step in this list.
- [ ] 2.9 Verify 2.2 the only way it can be verified: `docker compose up -d --force-recreate app` **without** touching the gateway, then run the flow again. A configuration that resolves at load time fails here and passes every other check in this group.

## 3. `cmd/identity-api`

- [ ] 3.1 New `cmd/identity-api/` (package `main`): `main.go` (configuration, pool, router, `http.Server`, `signal.NotifyContext`, shutdown) and an `identity.go` written from `cmd/api/identity.go`'s register/login half. It requires `IDENTITY_POSTGRES_DSN`, `IDENTITY_JWT_PRIVATE_KEY`, `IDENTITY_JWT_KEY_ID` and `IDENTITY_JWT_PUBLIC_KEYS` and reads nothing else. No Redis, no MinIO, no broker, no `ffmpeg`.
- [ ] 3.2 Routes: `POST /api/auth/register` and `POST /api/auth/login`, plus the same permissive CORS middleware. No bearer-auth group, no rate limiter — matching what `cmd/api` does for these two routes today. It is the only service that constructs an issuer.
- [ ] 3.3 `cmd/api` keeps `requireBearerAuth`, `authenticatedUserID` and the rest of `identity.go`: it still serves the video and notification routes, and a `package main` cannot import them from another. What leaves `cmd/api` in this group is only the two `/api/auth/*` route registrations, and only in 3.5, after the gateway is routing them elsewhere. `cmd/api`'s identity module keeps its verifier and loses its issuer, which is the first place the issuer/verifier split earns its keep.
- [ ] 3.4 Shutdown: `server.Shutdown` then close the pool. Nothing borrows the pool for a whole operation, so there is no ordering constraint to preserve here; say so in a comment rather than copying `cmd/api`'s sequence and leaving a reader to wonder what it is ordered against.
- [ ] 3.5 Gateway: route `/api/auth/` to `identity-api` and everything else to `app`. Remove the two routes from `cmd/api` in this same group once that rule is in place, so there is no window in which two processes serve the same path from two databases.
- [ ] 3.6 `cmd/identity-api/identity_test.go`: the register/login/token-shape assertions of `cmd/api/identity_test.go`, rewritten against a router this service builds — one identity module, no video module, no notification module, no limiter. Its `TestMain` requires only `IDENTITY_POSTGRES_TEST_DSN`; drop the `ffmpeg` and `VIDEO_MINIO_*` gate, which this suite genuinely does not need. The bearer-middleware tests do **not** come here: `requireBearerAuth` is not mounted by this service, and they belong to whichever service mounts it. Until group 6 that is still `cmd/api`, so they stay there and are rewritten in 4.6 and 6.8.
- [ ] 3.7 `Dockerfile`: build `/app/identity-api` in the builder stage and copy it into the runtime stage. `docker-compose.yml`: an `identity-api` service from the same image with `command: ["/app/identity-api"]`, no `ports`, with only the four identity variables.
- [ ] 3.8 `go vet ./...`, `go test ./... -v`, and the group 2.8 and 2.9 flows all pass.

## 4. `cmd/notification-api`

- [ ] 4.1 New `cmd/notification-api/` (package `main`): `main.go`, plus `notification.go`, `ratelimit.go`, and an `auth.go` carrying `requireBearerAuth`/`authenticatedUserID` over a verifier. All three are written from `cmd/api`'s copies and **`cmd/api` keeps its own** — it still serves the video routes, which need both middlewares. The duplication is transient by construction: group 6 deletes `cmd/api`, and what looks like three copies of the auth middleware ends as one per HTTP service, exactly as `rateLimitMiddleware` already is one thin gin wrapper per composition root over the shared `internal/platform/ratelimit`. The security-carrying half — algorithm pinning, `kid` lookup, indistinguishable rejection — stays single, in `internal/identity/infrastructure/jwtauth`.
- [ ] 4.2 It requires `NOTIFICATION_POSTGRES_DSN`, `REDIS_ADDR` and `IDENTITY_JWT_PUBLIC_KEYS`, and reads `NOTIFICATION_ALLOW_INSECURE_DESTINATIONS` and `RATE_LIMIT_*` as optional. It holds a **verifier only** — there is no code path in this binary that can construct an issuer, and no private key in its environment.
- [ ] 4.3 `REDIS_ADDR` is required because the preference routes are rate limited, not because this service caches anything. Comment it at the point it is loaded; it looks like a copy-paste otherwise and would be "cleaned up".
- [ ] 4.4 Routes: `GET`/`PUT /api/notification-preferences` on one group carrying `requireBearerAuth()` then `rateLimitMiddleware`, in that order — the invariant is the pair and its order, which `cmd/api` states and this service now carries alone. CORS advertises `GET, POST, PUT, OPTIONS` unchanged; the preflight test moves with the routes.
- [ ] 4.5 The destination policy is loaded here through the same `webhook.LoadDestinationPolicyFromEnv` that `cmd/notifier` uses. One parser, two readers — unchanged, and now across two processes that are separately deployable, which is the case the single-parser rule was written for.
- [ ] 4.6 `cmd/notification-api/notification_test.go` and `ratelimit_test.go`: the preference and limiter assertions of `cmd/api`'s, rewritten against this service's own fixtures — a local token minted with the test private key in place of `newTestIdentityModuleWithTokens`, and a local `alwaysAllowRateLimiter` in place of `video_test.go`'s. Bring the bearer-middleware assertions for the routes this service now owns. `ratelimit_test.go` is copied rather than moved, for the same reason the middleware is: every service carrying a copy of the middleware carries the tests that exercise it (see 6.6). `TestMain` requires `NOTIFICATION_POSTGRES_TEST_DSN` and `REDIS_ADDR`. The three cross-context pin tests stay in `cmd/api/notification_test.go` for group 5; `cmd/api`'s preference tests are deleted with the routes in 4.8.
- [ ] 4.7 Re-target `TestTheHTTPCompositionRootDoesNotLoadTheSecret` at `cmd/notification-api`'s own sources. It does not go to `internal/contracts`: it needs no Video Processing import, and its claim is strictly stronger here, since the other two services no longer link `internal/notification/infrastructure/postgres` at all.
- [ ] 4.8 Remove the preference routes and their tests from `cmd/api` and add the gateway rule for `/api/notification-preferences` in the same group, for the reason 3.5 gives. `cmd/api` keeps `notification.go`'s remaining wiring only if something still uses it; if nothing does, it goes here too.
- [ ] 4.9 `Dockerfile` builds `/app/notification-api`; `docker-compose.yml` gains the service, no `ports`.
- [ ] 4.10 `go vet ./...`, `go test ./... -v`, and the group 2.8 and 2.9 flows all pass.

## 5. `internal/contracts`

This group comes **before** the deletion of `cmd/api`, not after it. The three cross-context pin tests live in `cmd/api/notification_test.go`; a group that deletes that package first would ship an implementation PR with the contract guards silently gone and nothing left to move.

- [ ] 5.1 New `internal/contracts/doc.go`: the `package contracts` clause (a package comment alone is not a Go file) and above it a comment stating what the package is for — pinning cross-context integration contracts that no production process can pin any more — and that it contains no non-test declarations. Nothing else in the file.
- [ ] 5.2 Move `TestNotificationEventTypesMatchTheEmittedTerminalEventTypes`, `TestNotificationTerminalTopologyMatchesTheEmittedTopology` and `TestNotificationTerminalMessagesDecodeTheEmittedPayloads` here from `cmd/api/notification_test.go`, unchanged in what they assert.
- [ ] 5.3 Add a test in the package asserting the package itself declares nothing outside `_test.go` files — AST-walk its own directory, in the spirit of `internal/notification/dependency_rules_test.go`. Without it, "test-only" is a comment, and the exemption `ddd-architecture` grants this package is conditional on it being true. That emptiness is what makes the package undependable: a package exporting nothing cannot be imported for a symbol, so there is no useful dependency to form.
- [ ] 5.4 In the same test, also walk `cmd/` and `internal/` and assert no file imports `internal/contracts`. The self-walk above already makes a useful import impossible, but a blank import would compile, and the spec's claim is that nothing depends on it rather than that nothing usefully can. Two cheap assertions, one of them belt.
- [ ] 5.5 Confirm `internal/notification/dependency_rules_test.go` still passes unmodified: `internal/contracts` is not under `internal/notification/`, so the AST walk does not reach it and must not be widened to.
- [ ] 5.6 `go vet ./...` and `go test ./... -v` pass.

## 6. `cmd/video-api`, and the end of `cmd/api`

- [ ] 6.1 New `cmd/video-api/` (package `main`): `main.go`, plus `video.go`, `ratelimit.go` and `auth.go` written from `cmd/api`'s. It requires `VIDEO_POSTGRES_DSN`, `REDIS_ADDR`, the four `VIDEO_MINIO_*` variables, `RABBITMQ_URL` and `IDENTITY_JWT_PUBLIC_KEYS`. Verifier only, same as group 4.
- [ ] 6.2 Move `cmd/api/web/` to `cmd/video-api/web/` with its `go:embed` intact. `GET /`, `GET /styles.css` and `GET /app.js` are served by this service and reached through the gateway's default route. `app.js` is **not** modified: every URL it calls keeps its path and its origin. Confirm that by reading it, not by assuming it.
- [ ] 6.3 The dispatch outbox relay moves here unchanged, and with it the shutdown ordering that is not alphabetical: `server.Shutdown` → cancel the relay → **join** its goroutine → close the video pool → close Redis. The relay holds a transaction while it runs, so closing the pool before joining aborts an in-flight claim. Carry the existing comment; it is the only place in the new topology where the constraint still applies.
- [ ] 6.4 Routes: `GET /`, `POST /upload`, `GET /download/:filename`, `GET /api/status`, `POST /api/video-jobs`, `GET /api/video-jobs/:id`, `GET /api/video-jobs` — the bearer-auth group carrying `requireBearerAuth()` then `rateLimitMiddleware`, and the static assets outside it.
- [ ] 6.5 `GET /download/:filename`'s entitlement lookup keeps using the **undecorated** PostgreSQL repository rather than the cached one, unchanged. It is easy to lose in a move because it looks like an inconsistency; it is the fix for a stale `processing` cache entry 404-ing a result `GET /api/status` is already listing.
- [ ] 6.6 `cmd/video-api/ratelimit_test.go`: the six assertions of `cmd/api/ratelimit_test.go` — allowed request reaches the handler, denial returns `429` and never does, a limiter error fails open, an unresponsive limiter fails open inside the bounded timeout, an unauthenticated request never invokes the limiter, and two users are keyed independently — against **this service's** copy of the middleware. The video suite's own fixture is an always-allow fake, so without this file a regression in the Video API copy passes every test in the repository. A duplicated implementation is duplicated tests; that is the cost of the copy, and it is smaller than the alternative.
- [ ] 6.7 `cmd/video-api/video_test.go`: `cmd/api/video_test.go`'s assertions against this service's own fixtures, carrying `alwaysAllowRateLimiter` and the video module builders with it. `TestMain` requires `ffmpeg`, `VIDEO_MINIO_*`, `VIDEO_POSTGRES_TEST_DSN`, `REDIS_ADDR` and `RABBITMQ_URL` — the same fail-with-exit-1 posture, since `POST /upload` still stores into a bucket and a skipped suite would report green while covering none of it.
- [ ] 6.8 Rewrite the register→login→protected-route fixtures in this suite to mint a token directly with the test private key instead of calling `/api/auth/login`, and bring the bearer-middleware assertions for the routes this service now owns. The assertion is that a protected route accepts a valid token and rejects an invalid one; obtaining it through Identity's route tested Identity twice and is not available across a process boundary anyway.
- [ ] 6.9 Delete `cmd/api/` entirely — `main.go`, `main_test.go`, and anything left. Account for `main_test.go` first: every assertion in it either has a new home in one of the three suites or is deliberately dropped with a reason. Nothing in it may disappear silently.
- [ ] 6.10 `Dockerfile`: build `/app/video-api`, remove `/app/app`, and **change `CMD`**. It currently names `/app/app`, so removing that binary without touching `CMD` leaves an image that builds, passes every scan, and exits immediately under a plain `docker run` — the failure surfaces in `docs/operations.md`'s documented deployment command, not in the compose stack, which names its own command for every service. Set it to `/app/video-api` and rewrite `docs/operations.md`'s `docker run` commands accordingly (8.4): it is the service that serves the frontend, so it is the least surprising default, and every other process must still be named explicitly.
- [ ] 6.11 `docker-compose.yml`: remove the `app` service, add `video-api`, and repoint `app-test` — it builds the test stage and runs the whole module's suite, so its role is unchanged but its environment now needs every context's test DSN.
- [ ] 6.12 The gateway's default route now points at `video-api`. No path in `nginx.conf` still names `app`.
- [ ] 6.13 `go vet ./...`, `go test ./... -v`, and the group 2.8 and 2.9 flows all pass.

## 7. Verification (last implementation PR)

- [ ] 7.1 `go build ./cmd/identity-api`, `./cmd/video-api`, `./cmd/notification-api`, `./cmd/worker`, `./cmd/notifier` — five independent binaries, each built alone.
- [ ] 7.2 Start each of the three HTTP services on its own with only its own configuration and confirm it serves its routes. Then start each with a variable belonging to another service **removed** and confirm the other two are unaffected — the assertion is independence, not just startup.
- [ ] 7.3 Confirm `cmd/video-api` and `cmd/notification-api` cannot mint: grep proves nothing, so assert it by construction — neither binary's build graph contains a call to the issuer constructor, and `NewVerifier` refuses a private-key PEM (task 1.4). Neither service's environment carries `IDENTITY_JWT_PRIVATE_KEY`.
- [ ] 7.4 With `identity-api` **stopped**, confirm a previously-issued, unexpired token is still accepted by `cmd/video-api`. This is the property the configuration-distributed public key buys and the one JWKS would have cost; if it does not hold, the decision in `design.md` is wrong and the change is not done.
- [ ] 7.5 Recreate each backend service in turn (`docker compose up -d --force-recreate <service>`) without restarting the gateway, and confirm the flow still passes after each — the requirement is independent restart, and the gateway's DNS caching is the thing most likely to break it (2.2).
- [ ] 7.6 Full flow through the gateway on a clean `docker compose up --build`: register → login → upload a video larger than 1 MB → poll → status → download → redeem the presigned URL, in a browser as well as by curl, since the frontend is `go:embed`ed into a service that moved.
- [ ] 7.7 Confirm three workers still process three distinct videos concurrently (`run-multiple-workers-by-default`'s scenario) — nothing in this change touches the worker, and that is the assertion.
- [ ] 7.8 Confirm the per-user rate-limit budget is shared: exhaust it on `GET /api/status` and confirm `GET /api/notification-preferences` is refused too, from a different process against the same counter. Confirm also that `RATE_LIMIT_MAX_REQUESTS` and `RATE_LIMIT_WINDOW_SECONDS` are set identically on every service that mounts the limiter, from one place in `docker-compose.yml` — the counter is shared, so services holding different values would compare one count against two thresholds and the effective limit would depend on which route a request happened to take.
- [ ] 7.9 Confirm the three `TestMain`s together still satisfy `development-workflow`'s `Missing Test Prerequisites Must Cause a Non-Zero Exit`: run `go test ./...` with `ffmpeg` off `PATH`, then again with each context's test DSN removed in turn, and confirm each run exits non-zero naming the missing prerequisite. The requirement is written against `go test ./...`'s exit code rather than against any one `TestMain`, so splitting one gate into three satisfies it — but only if all three actually fail loudly, which is the thing to check rather than assume.
- [ ] 7.10 `gosec ./...` and `govulncheck ./...` clean.

## 8. Finalization

- [ ] 8.1 Check off every task above.
- [ ] 8.2 Name sweep across the canonical specs where `cmd/api` appears with no requirement change, per `design.md`: `videojob-result-storage`, `videojob-outbox-relay`, `videojob-worker`, `videojob-source-storage`, `rabbitmq-infrastructure`, `videojob-terminal-events`, `minio-infrastructure`, `videojob-lifecycle`, and `videojob-http-api`'s Purpose line. Substitute the service that actually runs the thing described; do not restate or re-scope a requirement while sweeping it.
- [ ] 8.3 `docs/architecture.md`: the request flow starts at the gateway, and there are five processes.
- [ ] 8.4 `docs/operations.md`: five services, the per-service variable table, the RSA key-pair generation step, the replacement of `IDENTITY_JWT_SIGNING_KEY` by `IDENTITY_JWT_PRIVATE_KEY`/`IDENTITY_JWT_KEY_ID`/`IDENTITY_JWT_PUBLIC_KEYS`, the rotation procedure the key set makes possible, the image's new default command and the requirement to name a binary for every other process, and the note that a deploy invalidates issued tokens.
- [ ] 8.5 `docs/development.md`: five `go run` targets and their configuration surfaces; `docker compose run --build --rm app-test go test ./... -v` unchanged.
- [ ] 8.6 `README.md` and `docs/flows.md`: the quickstart and the flow walkthrough, both of which name `IDENTITY_JWT_SIGNING_KEY`.
- [ ] 8.7 `CLAUDE.md`: the composition-root inventory, the request-flow numbering, the rate-limiting bullet, the JWT posture, and the frontend path.
- [ ] 8.8 `docs/roadmap.md`: the change's row.
- [ ] 8.9 `npx --yes @fission-ai/openspec validate split-api-by-bounded-context --strict --no-interactive` passes, then `/opsx:archive`.
